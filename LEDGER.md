# Daily Build Ledger

## 2026-06-28 — Prism
- **What:** A from-scratch software 3D rasterizer in pure Python implementing the exact pipeline GPUs execute in silicon: MVP matrix transforms, triangle rasterization via edge functions, perspective-correct attribute interpolation, z-buffering, backface culling, Blinn-Phong shading with multiple lights, PCF shadow mapping, MSAA 2×, OBJ/MTL loading, procedural textures, a from-scratch PNG encoder, and a self-contained HTML flipbook animation viewer.
- **Stack:** Pure Python 3 stdlib only (no NumPy, no PIL, no OpenGL); from-scratch PNG encoder using zlib + Sub filter; HTML flipbook with base64-embedded PNG frames.
- **Features shipped:** MVP pipeline (Model→View→Projection→NDC→Screen); edge-function triangle rasterizer with top-left fill rule and z-test; Blinn-Phong BRDF (ambient + diffuse NdotL + specular NdotH^shininess) in view space; multiple point/directional/ambient lights transformed to view space; perspective-correct UV/position/normal interpolation (attribute/w scheme, multiply by w_frag); near-plane Sutherland-Hodgman clip (w > near/2); PCF 3×3 shadow mapping with configurable bias; MSAA 2× supersampling with box resolve + Reinhard tonemapping + gamma correction; normal-visualization shading mode (view-space normal → RGB for debugging); five built-in scenes (torus, sphere, Cornell box, shadow demo, showcase); five procedural textures (checkerboard, marble, earth-like, UV grid, gradient); Wavefront OBJ/MTL loader with fan triangulation; from-scratch PNG encoder; HTML flipbook player + side-by-side comparison grid; full CLI (render/animate/obj/shadows/compare/info/demo); `demo.sh` that exercises all 9 feature areas.
- **Verdict:** Shipped. 4/4 required + 4 stretch features (textures, shadow mapping, MSAA, normal visualization); adversarial review (Phase 3) caught 4 bugs — shadow-map world positions recomputed per pixel instead of per triangle (orders-of-magnitude performance regression fixed by hoisting 3 matrix multiplies outside the pixel loop), `is_top_left` closure defined in innermost pixel loop (fixed by moving to module scope), `cmd_demo` saved a blank framebuffer PNG then immediately overwrote it (removed), dead `fb.triangles_rendered` attribute access (removed); 67/67 tests green (Vec3/Mat4, framebuffer, textures, geometry, camera, shader, rasterizer, PNG, full-pipeline smoke tests); demo.sh passes 13/13 checks.

## 2026-06-11 — Atlasforge
- **What:** Procedural fantasy world generator rendering interactive single-file HTML maps (terrain → climate → rivers → biomes → named settlements), deterministic per seed.
- **Stack:** Pure Python 3 stdlib (incl. hand-rolled PNG encoder), inline SVG/JS; jsdom for DOM tests only.
- **Features shipped:** deterministic fractal terrain w/ domain warp; priority-flood hydrology with never-dangling rivers + lakes; wind-advected climate w/ rain shadows; 18 Whittaker biomes; habitability-placed settlements w/ Markov names + founding history; interactive map (4 layers, pan/zoom, inspector, gazetteer fly-to); JSON export; CLI; 31-test suite + demo.sh.
- **Verdict:** Shipped. All 4 required + 3 stretch features done; adversarial review caught a broken default CLI path (fixed) — 31/31 tests green, ~2 s per 160×160 world.

## 2026-06-12 — RegexLab
- **What:** From-scratch regex engine (parser → Thompson NFA → Pike VM with captures → Hopcroft-minimized DFA) with an interactive single-file HTML visualizer that steps the VM over any test string, live threads highlighted in the real automaton.
- **Stack:** Pure Python 3 stdlib; generated HTML embeds a JS mirror of the Pike VM (no deps); Playwright/Chromium for browser tests only.
- **Features shipped:** full parser w/ positioned errors (greedy+lazy quantifiers, classes, anchors, `\b`); `match/fullmatch/search/finditer/findall` w/ groups — differentially fuzzed vs `re`, zero diffs incl. the 3.7 must-advance rule; subset-construction DFA + Hopcroft (Moore-cross-checked, textbook 4-state `(a|b)*abb`); interactive viz (tape, transport, keyboard, NFA+DFA views, capture table); `explain`, verified `gen`, first-class `fuzz` CLI; linear-time — `(a+)+$` ReDoS case in <1 ms.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review found 12 issues (worst: search died at dead spots, `\D` lost negation) — all fixed; 50/50 tests green incl. headless-Chromium JS≡Python parity.

## 2026-06-15 — PicoSQL
- **What:** A relational database engine from scratch in pure Python: SQL text → tokenizer → recursive-descent parser → planner → Volcano executor, running over a durable, paged B+tree in a single on-disk file (data survives restart in a hand-rolled binary format). Ships an interactive SQL shell.
- **Stack:** Pure Python 3 stdlib only (no deps); `sqlite3` used in tests for differential cross-checking.
- **Features shipped:** 4 KB-page storage w/ write-back cache, fsync, and a crash-safe rollback journal; B+tree w/ real leaf+internal node splits and leaf-linked range scans; type-tagged row encoding; durable table catalog; SQL parser w/ positioned caret errors (CREATE/INSERT/SELECT/UPDATE/DELETE/DROP); SELECT w/ projection, 3-valued-NULL WHERE, multi-key+positional ORDER BY, LIMIT/OFFSET, DISTINCT, IN/LIKE, scalar fns; GROUP BY + COUNT/SUM/AVG/MIN/MAX + HAVING + inner JOIN; query planner w/ PK index seek/range selection + EXPLAIN; BEGIN/COMMIT/ROLLBACK (byte-identical rollback); interactive REPL w/ box-drawn tables, .tables/.schema/.import(CSV)/.dump; statement-level atomicity.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review found 4 bugs (worst: `ORDER BY <int>` silently mis-sorted) — all fixed; 22/22 tests green incl. 4000-op B+tree oracle, simulated-crash recovery, and a 21-query differential suite vs sqlite3 (zero diffs).

## 2026-06-15 — Quorum
- **What:** A deterministic Raft consensus simulator — a cluster of from-scratch Raft nodes inside a seeded discrete-event network, with fault injection (crashes, partitions, loss/dup/reorder), a global safety-invariant monitor, and a linearizability checker. Same seed replays byte-for-byte, so any bug is 100% reproducible.
- **Stack:** Pure Python 3 stdlib (`dataclasses`, `heapq`, `random`, `argparse`, `unittest`); no threads/sockets/wall-clock, no third-party deps; ~2,350 LOC.
- **Features shipped:** correct Raft engine (election w/ up-to-date-log vote restriction, log replication w/ conflict backtrack, current-term commit rule + election no-op, term demotion); deterministic adversarial network w/ partitions checked at delivery time; runtime monitor for all 5 safety properties (Election Safety, Leader Append-Only, Log Matching, Leader Completeness, State Machine Safety); randomized chaos driver (100-seed sweep clean); Wing&Gong linearizability checker; live ASCII dashboard; log compaction + InstallSnapshot catch-up (exercised under chaos); 4 scripted hard-case scenarios; validated CLI. Mutation tests prove the monitor isn't vacuous.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review caught a real single-node election/commit bug, a snapshot-vs-append-only false positive, and a test gap where random chaos under-exercised the commit rule (added a deterministic Figure-8 regression) — all fixed; 27/27 tests green, 100-seed chaos sweep with zero safety violations and fully linearizable histories.

## 2026-06-15 — Tumble
- **What:** From-scratch 2D physics engine (Verlet integration + Position-Based Dynamics) with an interactive single-file Canvas playground — ropes, cloth, soft blobs and rigid boxes built from particles + distance constraints, draggable/cuttable/tearable in real time.
- **Stack:** Pure Python 3 stdlib (engine, CLI, SVG renderer, tests); a JavaScript mirror of the engine for the browser; Node for headless parity tests (no browser dependency).
- **Features shipped:** Verlet/PBD core (inverse-mass-weighted distance constraints, stiffness, pinning, fixed-step determinism); collisions (particle↔particle via uniform-grid broadphase, static segments, bounds w/ restitution+friction); composite bodies (rope/cloth/box/blob); interactive playground (drag-fling/cut/pin/spawn, sliders, stress-coloured links, HUD, trails); tearable cloth; JSON save/load + 8 presets; headless SVG renderer + `check` CLI; in-app SVG snapshot download.
- **Verdict:** Shipped. 4/4 required + 4 stretch; adversarial review fixed a zero-velocity drag + a reset-mid-drag crash; 27/27 tests green incl. free-fall vs analytic Verlet (exact), pendulum length <0.1%, box rigidity ~1e-4 px, and JS≡Python parity (max diff 0.0, bit-identical) across all 8 scenes.

## 2026-06-16 — Cotangent
- **What:** From-scratch reverse-mode automatic differentiation engine (scalar `Value` DAG + topological backprop) with a neural-net library, optimizers, synthetic datasets, finite-difference gradient checking, and two interactive single-file HTML visualizers (computation graph + live training playground).
- **Stack:** Pure Python 3 stdlib (no deps); generated self-contained HTML/SVG/JS for the visualizers.
- **Features shipped:** `Value` autodiff over `+ - * / ** -x` + `exp/log/relu/tanh/sigmoid` with correct shared-subexpression grads; central finite-difference gradient checker (analytic vs numeric, worst rel-err ~5e-11 across all ops + MSE/BCE/softmax-CE); `Neuron/Layer/MLP` + SGD(momentum,wd) + bias-corrected Adam; minibatched trainer reaching 100% on xor/moons/circles and 95.6% on a two-arm spiral; Adam-vs-SGD `compare`; computation-graph HTML (custom layered layout, no Graphviz) showing per-node value+grad; training-playground HTML (scrub/play decision boundary + loss/acc curve); CLI (`gradcheck/train/compare/viz/graph/demo`); ~60-check test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 3 stretch; adversarial review found 8 issues — the end-to-end demo caught my own spiral tuning as a cherry-pick (70% on the real default vs a claimed 95%), fixed honestly by retuning the dataset default and verifying the exact unmodified demo command hits 95.6%; ~60/60 checks green.

## 2026-06-16 — Backjump
- **What:** A modern CDCL SAT solver from scratch — DIMACS → two-watched-literal unit propagation → 1-UIP clause learning → non-chronological backjumping → VSIDS + Luby restarts — that returns SAT with a model or UNSAT with an independently machine-checked refutation, encodes real puzzles (Sudoku/N-Queens/colouring/pigeonhole) to CNF and decodes them back, and ships an interactive single-file HTML visualizer that replays a real solve (implication graph, 1-UIP conflict cut, learned clauses, VSIDS bars, restarts).
- **Stack:** Pure Python 3 stdlib (no deps); generated self-contained HTML/SVG/JS for the visualizer; Node/Playwright for headless replay + screenshot tests only.
- **Features shipped:** full CDCL engine (2-watched-literal BCP, 1-UIP analysis, non-chronological backjump, indexed-heap VSIDS w/ decay+rescale, phase saving, Luby restarts); DRUP-style UNSAT proofs verified by an independent RUP checker (no shared code) down to the empty clause; SAT model verifier; clause normalisation (dedup + tautology drop); Sudoku/N-Queens/graph-colouring/pigeonhole encoders+decoders; exact #SAT/AllSAT via blocking clauses; brute-force differential fuzzer + 3-SAT phase-transition demo (cliff at ratio≈4.26); 10-subcommand CLI w/ full stats and DIMACS I/O; 42-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review found 7 issues — worst was a real bug in the *proof checker* (it miscounted duplicate literals so a valid UNSAT proof got rejected once solver clause-normalisation diverged from it), plus root-level unit clauses not being propagated and UNSAT solves emitting no terminal trace event — all fixed; 24,000+ random instances agree with brute force with every model and proof independently verified; 42/42 tests green.
## 2026-06-16 — Conflux
- **What:** A from-scratch CDCL SAT solver (the algorithm behind industrial SAT) in pure Python, with certified answers — every SAT result ships a verified model, every UNSAT result a DRAT proof an independent checker validates — plus problem encoders and an interactive single-file HTML visualizer that animates conflict-driven clause learning step by step.
- **Stack:** Pure Python 3 stdlib (no deps); a JavaScript mirror of the engine drives the visualizer and is run under Node for Python↔JS verdict-parity tests; Playwright/Chromium for a headless UI smoke test only.
- **Features shipped:** CDCL engine (two-watched-literal propagation, VSIDS w/ decay, 1-UIP conflict analysis + recursive clause minimization, non-chronological backjumping, Luby restarts, phase saving); DIMACS I/O w/ line-aware errors; independent DRAT/RUP proof checker (accepts valid proofs, rejects bogus ones); encoders for Sudoku/N-Queens/graph-coloring/pigeonhole, each with decoder + standalone validator; interactive visualizer (trail by decision level, implication graph w/ highlighted 1-UIP, learned-clause stream, VSIDS bars, live sudoku grid, play/step/scrub + keyboard); random-3-SAT phase-transition sweep revealing the α≈4.26 threshold; ASCII `explain` conflict-anatomy mode; full CLI (`solve/sudoku/queens/color/php/gen/phase/explain/viz/demo`); 19-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; verified against a brute-force oracle (3000 random instances, 0 verdict mismatches, 0 bad models, 0 unverifiable proofs) and Python↔JS parity (0 diffs over 500 cases); adversarial review found 5 I/O-surface issues (worst: raw tracebacks on malformed input) — all fixed; solves AI-escargot and 25-queens in tens of ms; 19/19 tests green.
## 2026-06-16 — Crux
- **What:** A modern CDCL SAT solver written from scratch — two-watched-literals BCP, 1-UIP clause learning, non-chronological backjumping, VSIDS, phase saving, Luby restarts and clause deletion — plus problem encoders, a brute-force differential oracle, independently-checked UNSAT proofs, exact model counting, and an interactive HTML visualizer of conflict analysis.
- **Stack:** Pure Python 3 stdlib (no deps); generated self-contained HTML/SVG/JS visualizer; Node used only for a headless render test.
- **Features shipped:** full CDCL engine (watched literals, 1-UIP asserting-clause analysis, backjumping, VSIDS+decay, phase saving, Luby restarts, activity-based reduceDB, sound clause minimization, assumptions, conflict budget) with verified models + stats; tolerant DIMACS parser/writer + model checker; brute-force oracle + fuzzer (differentially verified, zero disagreements over 10k+ random formulas); Sudoku/graph-colouring/N-Queens/pigeonhole encoders+decoders with Sinz sequential at-most-one; exact #SAT model counter (brute-cross-checked); DRUP/RUP UNSAT-certificate checker sharing no solver code; interactive conflict-analysis visualizer drawing the real implication graph, ⊥ node, 1-UIP cut and learned clause; full CLI (`solve/check/gen/encode/decode/fuzz/count/proof/viz/demo`); 27-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review confirmed the engine correct (10k-formula differential fuzz, zero mismatches) but caught a 1883× VSIDS heap blow-up (105k entries for 56 vars → bounded O(nvars)), a dead/misleading `rng_seed` param (removed), and a missing conflict budget (added UNKNOWN status) — all fixed with regressions; 27/27 tests green, real 9×9 Sudoku solved in ~20 ms, pigeonhole UNSAT certified by the independent checker.
## 2026-06-17 — Resolvent
- **What:** A from-scratch CDCL (Conflict-Driven Clause Learning) SAT solver — the modern MiniSat-style architecture in pure Python — that decides Boolean satisfiability and doubles as a general reasoning engine (Sudoku, graph coloring, N-Queens, pigeonhole), with every UNSAT answer independently re-checked by a separate proof verifier.
- **Stack:** Pure Python 3 stdlib only (no deps); generated self-contained HTML/SVG for the implication-graph visualizer.
- **Features shipped:** strict line-numbered DIMACS parser/writer (tautology+dupe handling, multi-line clauses); CDCL core with two-watched-literal unit propagation, 1-UIP conflict analysis + clause learning, non-chronological backjumping, self-subsuming minimization; VSIDS branching w/ decay, phase saving, Luby restarts, activity-based clause-DB reduction; `solve` CLI with model self-verification + full stats (SAT-competition exit codes); problem encoders (Sudoku/coloring/queens/PHP → CNF and decoded back, solutions validated); independent RUP UNSAT-proof checker sharing no search logic; differential fuzzer vs exhaustive truth-table + recursive-DPLL oracles; implication-graph HTML/SVG viz (decisions, implications, conflict κ, highlighted 1-UIP cut + learned clause); self-checking `demo`; 27-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review caught a CRITICAL VSIDS-heap bug that orphaned variables and returned *false SAT* on 8–13-var instances (fixed via a clean "every unassigned var always has a current heap entry" invariant), plus a parser error-location wart — after the fix 0 mismatches/0 invalid models across 6,000 random instances; 27/27 tests green, both UNSAT oracles agree, PHP proofs RUP-verified.
## 2026-06-16 — Resolvent
- **What:** A from-scratch CDCL SAT solver in pure Python — conflict-driven clause learning over a two-watched-literal core — that decides Boolean formulas SAT (with a verified model) or UNSAT (with an independently machine-checkable proof), plus encoders that compile Sudoku/N-Queens/graph-coloring/pigeonhole down to CNF and an interactive implication-graph visualizer.
- **Stack:** Pure Python 3 stdlib (no deps); generated self-contained HTML/SVG/JS for the visualizer.
- **Features shipped:** tolerant DIMACS parser w/ positioned errors + serializer + CNF builder; CDCL engine (trail/decision levels, two-watched-literal unit propagation, 1-UIP conflict analysis + clause learning, non-chronological backjumping); VSIDS branching w/ decay + phase saving + Luby restarts + LBD/activity clause-DB reduction (VSIDS off ≈ 7× more conflicts); brute-force truth-table oracle + 1500-case differential fuzzer + per-model verification; problem encoders (queens/sudoku/graph-color/pigeonhole) with model→board decoders; DRUP UNSAT-proof logging + a fully independent RUP proof checker; dependency-free implication-graph HTML (custom layered layout, decision/propagation/conflict nodes, 1-UIP cut, pan/zoom/hover); CLI (`solve/gen/check/fuzz/bench/encode/solve-puzzle/viz/proofcheck/demo`) w/ SAT-competition exit codes; 29-test suite + demo.sh; phase-transition sweep reproducing the ~4.26 P(SAT) collapse.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review caught a soundness bug (trivial/load-time UNSAT emitted an empty, unverifiable DRUP proof — fixed to log the empty clause), a crash in `check`, and dead viz code; 29/29 tests green incl. a 1500-instance brute-force differential gate (zero verdict mismatches, zero bad models) and 25+ independently-verified UNSAT proofs.

## 2026-06-16 — Resolvent (branch bpmxt2 — folder `2026-06-16-resolvent-bpmxt2/`)
> Note: a second independent build also named "Resolvent" landed the same day; to avoid clobbering the folder `2026-06-16-resolvent/`, this one was merged under `2026-06-16-resolvent-bpmxt2/`.
- **What:** A from-scratch CDCL (conflict-driven clause-learning) SAT solver with a constraint-modeling DSL, four puzzle compilers, an independent UNSAT proof checker, and an interactive single-file HTML implication-graph visualizer. One engine compiles Sudoku/N-Queens/coloring/pigeonhole to CNF, solves them, and decodes back; UNSAT answers come with a machine-checkable certificate.
- **Stack:** Pure Python 3 stdlib (no deps); generated self-contained HTML/SVG/JS for the visualizer (Node used only in dev to syntax/runtime-check the generated JS).
- **Features shipped:** full CDCL core (two-watched-literal BCP, VSIDS w/ lazy heap, 1-UIP analysis + recursive self-subsuming minimization, non-chronological backjump, phase saving, Luby restarts, reduceDB w/ clause-locking + index remap); modeling layer (Tseitin AND/OR/NOT/XOR/IMPLIES/IFF + cardinality at-most/least/exactly-one and at-most-k Sinz sequential counter, all exhaustively verified); Sudoku (any N²)/N-Queens/graph K-coloring/pigeonhole compilers + decoders; verification (every SAT model re-checked, brute-force oracle, exact #SAT DPLL counter, independent DRAT/RUP UNSAT proof checker that rejects tampered proofs); interactive implication-graph HTML (layered layout, pan/zoom, conflict stepper, 1-UIP cut highlight, stats — no Graphviz); random k-SAT generator; CLI (`solve/sudoku/queens/color/php/gen/verify/viz/demo`); 35-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 3 stretch; adversarial review caught a real reduceDB clause-reindexing landmine (stale reason pointers, correct-only-by-luck — fixed with clause locking + index remap) plus 6 edge cases; 35/35 tests green, 2,500-instance differential vs brute force clean, 974 UNSAT proofs independently verified, PHP(9,8) UNSAT through 77 reduceDB cycles with a valid proof.

## 2026-06-17 — Cinch
- **What:** A from-scratch data-compression toolkit in pure Python implementing the real algorithms behind modern compressors — canonical Huffman, LZ77, a DEFLATE-style combined codec, and an adaptive range (arithmetic) coder — behind a self-describing, CRC-checked container format, a CLI, and an interactive single-file HTML visualizer.
- **Stack:** Pure Python 3 stdlib only (no `zlib`/`bz2`/`lzma` inside any codec — they appear only as an external ratio yardstick in `bench`); generated self-contained HTML/SVG/JS visualizer; Node used only in dev to runtime-check the generated JS.
- **Features shipped:** canonical Huffman with **package-merge** length-limited optimal codes (verified optimal vs textbook Huffman) + RLE'd code-length header; LZ77 with a 32 KB-window hash-chain match finder, one-step lazy matching, and exact overlapping-copy decoder; **DEFLATE-lite** composing LZ77 ∘ Huffman over the genuine DEFLATE length/distance symbol alphabets with extra bits and two per-file dynamic Huffman trees; carryless 32-bit Subbotin **range coder** with adaptive order-0 and order-1 models (order-1 beats our own Huffman on text); from-scratch CRC-32 (bit-identical to `zlib.crc32`); self-describing `.cinch` container with method dispatch + `auto` (never worse than `store`); CLI (`compress/decompress/inspect/bench/roundtrip/viz/demo`); interactive HTML visualizer animating the LZ77 sliding window + drawing the canonical Huffman tree; 24-test unit/property/differential/fuzz suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review caught 5 bugs — worst was a single-symbol-tree desync that silently corrupted DEFLATE (`b"a"*5000` → 10160 bytes) and a decoder that **hung forever** on corrupt input (range coder reading infinite zero-padding past EOF) — all fixed with output-bound guards and regressions; 24/24 tests green (incl. a 4000-case all-methods fuzz and a 1216-flip corruption sweep with zero silent-wrong decodes), demo round-trips every method byte-for-byte, deflate lands near zlib on text.

## 2026-06-17 — Shannon
- **What:** A from-scratch lossless data-compression toolkit in pure Python implementing the three pillars of real compressors — entropy coding (canonical Huffman + a Witten–Neal–Cleary arithmetic coder with adaptive order-0/order-1 models), dictionary coding (LZ77/LZSS with a Huffman backend), and reversible transforms (suffix-array BWT + MTF + RLE) — composed into six named codecs over a self-verifying `.shz` container, with an entropy/codec comparison report and a self-contained interactive HTML visualizer.
- **Stack:** Pure Python 3 stdlib only (no zlib/bz2/lzma, no deps); `zlib.crc32` used solely as the container checksum; generated self-contained HTML/SVG/CSS/JS for the visualizer; Playwright/Chromium for screenshots only.
- **Features shipped:** MSB-first bit I/O + LEB128 varints; canonical Huffman (table-free decode, Kraft-verified, single-symbol/empty edge cases); 32-bit WNC arithmetic coder with carry/underflow handling + Fenwick-tree adaptive order-0 and order-1 context models (≈+2.5% over the H₀ bound, order-1 beats order-0 on text); LZSS sliding-window matcher (32 KiB window, 3-byte hash chains) with Huffman + gamma-coded litlen/length/distance token streams; suffix-array BWT (sentinel forward, LF-mapping inverse) blocked at 32 KiB + MTF + PKZIP-style RLE → "bzip-lite"; `.shz` container (magic/method/orig_len/CRC) with self-verifying decompress; hardened tolerant decoder; entropy + ranked codec-comparison report with `--best` auto-select; four-tab interactive HTML report (codec bars, real canonical Huffman tree, sorted BWT rotation matrix, LZ77 token map); CLI (`compress/decompress/info/analyze/viz/demo`) with sane exit codes; 28-test suite + self-checking `demo` + demo.sh.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch; adversarial review found a CRITICAL decompression-bomb OOM (a corrupt length/count varint drove unbounded allocation — fixed with a 1 GiB ceiling + derived/validated block count + bounded symbol count) and uncaught corrupt-input exceptions (normalized to clean ValueErrors); verification later caught a flawed CRC test that flipped only trailing zero-padding bits (corrected to corrupt consumed data). 28/28 tests green, 1,000-case differential fuzz across all codecs with zero round-trip failures, and a 600-case corruption sweep with zero crashes and zero undetected-wrong outputs.

## 2026-06-18 — Coil
- **What:** A complete, from-scratch dynamically-typed programming language with a real bytecode virtual machine — source → lexer → Pratt parser → AST → single-pass-ish bytecode compiler → stack VM — featuring first-class functions, closures that capture upvalues by reference, lists & maps, a native standard library, line-numbered runtime tracebacks, a bytecode disassembler, a REPL, and its own mark-and-sweep garbage collector over a VM-managed object heap.
- **Stack:** Pure Python 3 stdlib only (no deps); `unittest` for the suite.
- **Features shipped:** character lexer (line/col, string escapes, nested block comments, positioned errors); Pratt/precedence-climbing parser with caret-annotated syntax errors; bytecode compiler with global/block-scoped-local resolution (slots), upvalue capture, jump back-patching, and correct `break`/`continue` scope unwinding; stack VM (arithmetic, string & list `+`, comparisons, short-circuit `and`/`or`, `if`/`else if`/`while`/`for`, `print`); functions/recursion/closures (counter factory, per-iteration loop capture, arbitrary nesting; iterative call loop so deep recursion never hits Python's limit); lists & maps with literals, get/set indexing (incl. negative indices) + `.` sugar; ~25 native builtins (`len/str/num/type/push/pop/keys/values/has/delete/range/slice/abs/floor/ceil/sqrt/min/max/split/join/upper/lower/contains/clock/assert/gc/heap_size`); mark-sweep GC tracing the real root set with `--gc-stress`/`--trace-gc`; bytecode disassembler (`--disasm`); interactive REPL with expression echo + multi-line buffering; multi-frame source-annotated runtime tracebacks; 5 example programs; 87-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review caught a CRITICAL `break`/`continue` bug (jumped without popping loop-body locals → value-stack corruption, a probe returned 500 instead of 800 — fixed by emitting scope-unwind POP/CLOSE_UPVALUE before the jump) and a GC un-tracking hazard (`BUILD_LIST`/`BUILD_MAP`/list-`+` allocated the aggregate after un-rooting its elements, so the collector swept them out of the managed heap — fixed by allocating while operands stay on the stack), both with regressions; 87/87 tests green incl. closures under `--gc-stress`, deep recursion, and all 5 example programs.

## 2026-06-18 — Gambit
- **What:** A complete chess engine from scratch in pure Python — 0x88 board, fully-legal move generation verified to the node against published perft counts, alpha-beta + quiescence search with a Zobrist transposition table, a tapered piece-square evaluation, SAN/FEN/PGN I/O, a terminal play mode, a **server-backed browser board where you play the real Python engine** (no JS engine), and a self-contained HTML game viewer with an eval graph.
- **Stack:** Pure Python 3 stdlib only (no deps, no network); stdlib `http.server` for the interactive board; generated self-contained HTML/SVG/JS for the viewer + board.
- **Features shipped:** 0x88 movegen (sliders/leapers, castling w/ through-check + rights, en passant incl. discovered-check, promotions, pins) **perft-exact on startpos/Kiwipete/positions 3–5**; negamax alpha-beta w/ iterative deepening, quiescence (captures + check evasions), MVV-LVA + killer + TT-move ordering, depth/time budgets; tapered eval (material + PSTs + king-safety taper + bishop pair + doubled/isolated/passed pawns + pawn shelter), colour-symmetric; SAN (full disambiguation + `+`/`#`) / FEN (validated) / minimal PGN, lossless round-trip w/ 0 collisions over 6,000 positions; incremental 64-bit Zobrist TT w/ depth-preferred replacement; stdlib HTTP server to play the real engine in-browser; static HTML viewer (eval bar+graph, clickable movelist, captured trays, keyboard scrub); CLI (`perft/analyse/selfplay/play/viz/serve/demo`); 22-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review found 6 issues — the worst, caught in Phase-5 verification, was a CRITICAL board-corruption bug where an aborted *timed* search unwound through `make_move()` without the matching `unmake_move()`, returning a legal best move on a mutated board (invisible to deterministic depth-only play) — fixed by restoring the undo-stack baseline on abort; also tightened an over-eager insufficient-material draw, added depth-preferred TT replacement, FEN/king-count validation and clean CLI errors. 22/22 tests green; perft exact on all 5 positions; 27k-move make/unmake+Zobrist fuzz with zero drift.

## 2026-06-18 — Galley
- **What:** Optimal typesetting from scratch — the **Knuth–Plass total-fit line breaker** (the algorithm that sets every professionally typeset book) with **Liang hyphenation**, real Adobe **AFM font metrics**, and a box/glue/penalty engine, rendering justified type to SVG / multi-paragraph page / HTML / greeked PNG / ASCII. Breaking a paragraph becomes a shortest-path problem over feasible breakpoints whose global minimum-demerit solution is proven optimal against a brute-force oracle.
- **Stack:** Pure Python 3 stdlib only (no deps), incl. a hand-rolled PNG encoder (zlib+CRC); generated self-contained HTML/SVG/JS for the interactive playground; Node used only in tests to gate Python↔JS parity.
- **Features shipped:** box/glue/penalty node model + tokenizer w/ real inter-word glue; full Knuth–Plass DP (cubic badness, demerits w/ penalty + double-hyphen + fitness-class terms, active-node pruning, forced/flagged breaks, per-line measures, TeX-style tolerance escalation); Liang hyphenation w/ a genuine Knuth–Liang pattern subset + exception list + left/right-min + punctuation-aware tokenizing; pixel-exact justified SVG (`textLength`), multi-paragraph page SVG, styled HTML compare doc, monospace ASCII column; greedy first-fit + apples-to-apples demerit scorer; interactive HTML playground (JS port re-breaks live as you drag the measure); exhaustive brute-force optimality oracle; dependency-free greeked PNG preview; whitespace-river detector; CLI (`hyphenate/break/render/compare/preview/playground/rivers/demo`); 42-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 4 stretch; adversarial review caught a *fake feature* (a `looseness` param that silently did nothing — removed, replaced with punctuation-aware hyphenation), plus empty-`--text` falling back to the sample, overfull ASCII lines being truncated (hiding text), and an over-loose river detector; core proven optimal — 0 mismatches vs brute force across thousands of random paragraphs; round-trip integrity and Python↔JS parity hold; 42/42 tests green.

## 2026-06-23 — Lumina
- **What:** A Monte Carlo path tracer built from scratch in pure Python — solves the rendering equation via unbiased Monte Carlo integration, producing physically correct global illumination, soft shadows, color bleeding, and caustics.
- **Stack:** Pure Python 3 stdlib only (no deps, no numpy, no imaging libs); hand-rolled PNG encoder with CRC-32; Wavefront OBJ loader.
- **Features shipped:** Monte Carlo path tracing with Russian Roulette termination; **Next-Event Estimation + MIS balance heuristic** (explicit light sampling + BRDF sampling weighted by solid-angle PDF, ~4–8× variance reduction vs BRDF-only); **BVH with SAH splits** (prefix/suffix bounding boxes, O(N) split evaluation, iterative traversal — handles 400+ sphere scenes); **PBR materials** — Lambertian (cosine-weighted hemisphere, pre-computed weight = albedo, no PDF division), Metal (mirror + fuzz), Dielectric (Snell's law + Schlick Fresnel + TIR), Emissive (area lights with `_sample_area` annotation for MIS PDFs); **Möller–Trumbore** triangle intersection with barycentric smooth-normal interpolation; Reinhard + ACES filmic tone mapping (gamma 2.2); hand-rolled PNG encoder + decoder (round-trip verified); **bilateral denoiser** (spatial × range Gaussian); **Wavefront OBJ loader** (v/vn/f, fan triangulation, smooth normals, flip_normals); JSON scene format (sphere/triangle/quad_light/box types); built-in scenes (Cornell box, showcase, glass, 400+ sphere BVH stress test); CLI (`render/info/bench/obj`); 65-test suite (Vec3, BVH correctness vs brute-force over 500 rays × 200 spheres, Fresnel boundary conditions, PNG round-trip, tone-map clamping, OBJ loader, denoiser variance reduction); demo.sh (23 checks: R1–R4 required + S1–S3 stretch + JSON loading).
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review caught a CRITICAL physics bug (renderer was dividing throughput by PDF *after* scatter() had already pre-computed the weight, multiplying luminance by π/cos(θ) per bounce — Cornell box mean luminance was 1758, should be ~0.09), a QuadLight area computed twice, an OBJ flip_normals double-negation (net no-flip), a missing renders/ directory creation, a front-wall Cornell box blocking the camera, a NEE MIS gap (BRDF-sampled paths that hit lights through gaps were given zero weight when prev_specular=False — fixed by tracking prev_pdf and computing solid-angle PDFs via annotated _sample_area), and a single-object BVH crash in the info command; 23/23 demo checks pass, 65/65 tests green.

## 2026-06-18 — Gambit (branch 8qd3ko — folder `2026-06-18-gambit-8qd3ko/`)
> Note: a second independent build also named "Gambit" landed 2026-06-18; to avoid clobbering the folder `2026-06-18-gambit/`, this one was merged under `2026-06-18-gambit-8qd3ko/`.
- **What:** A complete chess engine written from scratch in pure Python — a 0x88 mailbox board with every rule of chess, a perft-verified legal move generator, a Principal Variation Search (iterative deepening + quiescence + null-move + transposition table), a tapered piece-square evaluation, a play-against-it CLI with PGN export, and a self-contained HTML viewer that replays the engine playing itself with a live evaluation graph.
- **Stack:** Pure Python 3 stdlib only (no deps, no chess libraries); generated self-contained HTML/Canvas/JS for the replay viewer (Node used only in dev to runtime-check the generated JS).
- **Features shipped:** 0x88 board w/ full state, strict-validating FEN parse+serialize (round-trips, rejects malformed/king-less boards), incremental Zobrist (== recompute over millions of moves), bit-exact make/unmake/null; fully-legal movegen (all promotions+capture-promos, castling through/into-check rejection, en-passant incl. EP discovered-check) **verified by perft to Kiwipete depth 4 = 4,085,603 and startpos depth 5 = 4,865,609**; PVS w/ zero-window scout + re-search, check-aware quiescence, null-move pruning (zugzwang-guarded), MVV-LVA + killer + TT-move ordering, `#N` mate reporting; tapered material+PST eval, exactly color-anti-symmetric; move-ordering-only TT that is provably score-neutral (TT-on==TT-off) yet cuts ~25% of nodes; interactive HTML self-play replay (Unicode board, last-move highlight, scrubbable eval graph, clickable SAN list, keyboard transport, embedded PGN); human-vs-engine + selfplay CLI w/ SAN/coordinate parsing and PGN export; `perft/analyze/play/selfplay/viz/demo` CLI; 26-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review caught a CRITICAL unsound TT (bound-cutoffs made the root score — and potentially the chosen move — depend on the TT: 520 vs 513 on a probe position) fixed by reducing the TT to provably score-neutral move-ordering + PVS, plus king-less-FEN silent acceptance, a check-blind quiescence, and a 1cp color bias from floor-division in the eval — all fixed; 26/26 tests green, perft exact on all five reference positions, and the engine solves mates, wins hanging material, and self-mates a KQ-vs-K endgame (…9.Kg6 Kg8 10.Qd8#).

## 2026-06-19 — Lumen
- **What:** A physically-based path tracer built from scratch in Python — Monte Carlo global illumination with indirect bounces, color bleeding, soft shadows, glass caustics, and depth-of-field bokeh, all emerging from a single 50-line integrator loop. Outputs valid PNG renders via a stdlib-only encoder (no Pillow).
- **Stack:** Python 3 + numpy (math only); zero external dependencies for rendering; stdlib struct + zlib for PNG encoding; multiprocessing.Pool for tiled rendering.
- **Features shipped:** Geometry: Sphere, Quad, Box, Translate, RotateY + median-split BVH (AABB slab test, O(log N) traversal); Materials: Lambertian (cosine-weighted hemisphere sampling via ONB), Metal (specular reflection + roughness fuzz), Dielectric (Snell's law refraction + Schlick Fresnel, models glass/water), DiffuseLight (area light — the only light source); Path tracer: unbiased Monte Carlo with Next Event Estimation for diffuse surfaces (shadow rays to lights), Russian roulette termination after depth 3, anti-aliasing via sub-pixel jitter; Camera: perspective + thin-lens depth of field (aperture + focus distance); Textures: SolidColor, CheckerTexture (3D sine), PerlinTexture (gradient Perlin noise → marble/turbulence); Scenes: Cornell box (two rotated boxes + glass sphere), metallic spheres (DoF bokeh + checker + marble), glass caustics; JSON scene loader; ACES filmic tone mapping + gamma 2.2; tiled multiprocessing renderer; HTML gallery (embedded base64 PNGs, dark-mode, render stats).
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review found 2 real bugs — (1) `cmd_render` for custom JSON scenes reverse-engineered camera parameters from a Camera object using broken math (fixed: parse JSON directly), (2) `_DEFAULT_CAMS` shared specs were mutated in place causing wrong aspect ratios on subsequent renders (fixed: `copy.copy()` before mutation); 56/56 tests green covering all geometry, materials, transforms, energy conservation, PNG round-trip, and end-to-end color correctness for all three scenes; demo renders confirmed: cornell.png 96×96 (59s), spheres.png 128×72 (5s), caustic.png 128×72 (17s) on 4 cores.

## 2026-06-20 — Lumina
- **What:** A complete physically-based renderer from scratch in pure Python — unidirectional Monte Carlo path tracer with Russian Roulette, Whitted recursive ray tracer with Phong shading, SAH BVH acceleration, five material types, thin-lens depth of field, OBJ mesh loading, a from-scratch PNG encoder with ACES/Reinhard tonemapping, and an interactive HTML scene editor.
- **Stack:** Pure Python 3 stdlib only (zlib for PNG compression, `multiprocessing` for parallel rendering, `http.server` for the viz editor); no Pillow, no NumPy, no ray-tracing libraries.
- **Features shipped:** iterative path tracer (Russian Roulette, energy-conservative — white furnace test passes at ≈1.0); recursive Whitted tracer (Phong + metal + dielectric, AreaLight stratified soft shadows); SAH BVH (prefix/suffix surface-area split, ~70× speedup on 1025-triangle mesh, zero BVH/brute-force disagreements over 200 rays); Lambertian (cosine-weighted ONB hemisphere), Metal (fuzz), Dielectric (Snell+Schlick), Phong, Emissive; Sphere, Plane, Quad, Triangle (Möller-Trumbore, one/two-sided), Box (6 quads with verified outward normals), PrimitiveList; thin-lens DOF (all lens-offset rays converge within 0.02 units at focal plane); OBJ loader (fan-triangulation, strict index validation, ValueError on index 0); from-scratch PNG encoder (IHDR+IDAT+IEND, zlib level-6); ACES filmic + Reinhard + clamp + gamma tonemapping with explicit NaN/Inf guards; multiprocessing row-batch render with single-threaded fallback; JSON scene format; interactive HTML viz editor (sphere JS preview + POST /render); 6 bundled scenes + 2 OBJ meshes; CLI (`render/bench/viz/demo`); 41-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 2 stretch (BVH SAH + interactive viz editor); adversarial review caught 8 bugs — the worst were: `Vec3.__imul__` silently stored a Vec3 object into `self.x` instead of doing component multiply (corrupted all path-traced throughput), NaN pixels rendered as white 255 because `min(1.0, NaN) == 1.0` in Python (fixed with explicit `math.isnan` guards), all Box face normals pointing inward so `front_face` was always False for exterior rays (fixed by reconstructing all 6 faces with correct u×v cross products), and Russian Roulette creating energy (`survive` was computed before applying attenuation — fixed by applying `throughput *= attenuation` first); 41/41 tests green incl. white furnace, BVH differential, DOF geometry, Snell's law, and end-to-end render smoke tests.

## 2026-06-21 — Pathtracer
- **What:** A Monte Carlo path tracer written from scratch in pure Python 3 (stdlib only — no NumPy, no Pillow) — recursive stochastic light transport with physically-based materials, BVH acceleration, thin-lens depth-of-field, Perlin texture noise, fork-based parallel tile rendering, a hand-rolled PNG encoder, and a live progressive browser preview via Server-Sent Events.
- **Stack:** Pure Python 3 stdlib only (`math`, `random`, `zlib`, `struct`, `multiprocessing`, `http.server`, `threading`); no external deps of any kind.
- **Features shipped:** recursive Monte Carlo path tracing with Russian roulette unbiased termination; **three PBR materials** — Lambertian (cosine-weighted hemisphere via Malley's method), Metal (mirror + roughness fuzz), Dielectric (Snell's law + Fresnel-Schlick, hollow glass via negative radius); DiffuseLight area light for soft shadows + color bleeding; BVH with **SAH** (Surface Area Heuristic, 12-bin sweep) — 3× speedup over brute-force on 100 spheres in Python; thin-lens camera with aperture and focus distance; three procedural textures (solid, checkerboard, Perlin gradient noise / marble); four primitive types (Sphere, axis-aligned Box, infinite Plane, Triangle via Möller-Trumbore); fork-based parallel tile renderer (`multiprocessing.Pool`); hand-rolled PNG encoder (Sub filter, CRC-32, IHDR/IDAT/IEND); **progressive browser viewer** serving live tile-by-tile preview via SSE + canvas; five-subcommand CLI (`render/demo/bench/info/view`); four built-in scenes (classic spheres, Cornell box w/ color bleeding, DoF bokeh demo, 100-sphere random field); `gen_random_spheres.py` for N-sphere scenes; 113-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review (Phase 3) caught 5 bugs — CRITICAL: hollow-glass sphere normals inverted by negative radius (fixed with `abs(radius)` in normal computation), Box.hit returned None for inside-box rays (fixed with exit-face fallback), spp=0 division by zero (clamped); MINOR: schlick() cosine unbounded, Metal scatter unnormalized; Cornell box ceiling occlusion found and fixed by moving the ceiling above the light. 113/113 tests green; demo.sh runs all 5 subcommands end-to-end.

## 2026-06-22 — Coda
- **What:** A from-scratch MIDI synthesizer and procedural music generation system in pure Python — binary MIDI parser (Type 0/1, VLQ, running status, all channel + meta events), multi-waveform synthesizer (sine/square/sawtooth/triangle/noise with band-limited additive harmonics), ADSR envelopes, 10 GM-mapped instrument timbres, a complete MIDI-to-WAV renderer with a piecewise-linear tempo map, a procedural music generator (7 scales, diatonic chord progressions, Markov melody, walking bass, 4 drum styles), a 3-effect DSP chain (Biquad LPF, Schroeder reverb, ping-pong delay), and a self-contained HTML piano roll visualizer with Web Audio API synthesis and playback.
- **Stack:** Pure Python 3 stdlib only (no deps, no numpy, no scipy, no mido); generated self-contained HTML/JS/CSS for the visualizer using Web Audio API.
- **Features shipped:** 44100 Hz 16-bit stereo WAV output + read-back; sine/square/sawtooth/triangle/noise waveforms (square+saw band-limited via additive synthesis); ADSR envelopes with correct attack/decay/sustain/release shape; constant-power stereo pan; binary MIDI parser with VLQ decoder, running status, NoteOn-vel=0→NoteOff, all channel events, all meta events; MIDI writer with lossless tick round-trip; piecewise-linear tempo map with multiple Tempo Change support; 10 instrument presets from additive + FM synthesis (piano, strings, organ, brass, flute, marimba, …) + GM drum map with 14 notes; Markov melody (stepwise motion weighted, 40% chord-tone gravity); diatonic chord progressions from a Roman-numeral library; walking bass on roots/fifths/thirds; 16th-note drum grid (4 styles) with ghost notes and crash hits; Biquad LPF (bilinear transform); Schroeder reverb (4 comb + 2 allpass, stereo spread); ping-pong delay; piano roll HTML (color-coded tracks, piano keyboard, tooltip, seek, zoom, animated playhead); `generate/render/make/viz/info/demo` CLI; 86-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch; adversarial review found 2 critical bugs (bass notes at MIDI 96-103 C7-G7 due to `+36` offset instead of `-24`; chord voicings at MIDI 108-120 due to `+48` offset instead of `0`) and 3 moderate issues (normalize only clipped, never boosted; empty bass pool fallback; wrong module import) — all fixed; 86/86 tests green covering WAV I/O, VLQ edge cases, MIDI binary parsing, ADSR shape, frequency accuracy, scale adherence (0% off-scale), note register correctness, reverb/delay stability, and CLI error handling.

## 2026-06-24 — Cadence
- **What:** A modular music synthesizer and procedural composer from scratch in pure Python — PolyBLEP-antialiased oscillators, ADSR envelopes, 2nd-order biquad IIR filters, Freeverb stereo reverb, LFO modulation, a Cooley-Tukey FFT, a from-scratch WAV encoder/decoder, and a four-track step sequencer that generates full musical compositions in four styles (pop, jazz, ambient, electronic) with melody, bass, pads, and drums. An HTML visualizer renders a waveform canvas, log-spaced FFT spectrum, and piano roll for each piece.
- **Stack:** Pure Python 3 stdlib only (no NumPy, no scipy, no `wave` module, no `sounddevice`); generated self-contained HTML/Canvas with base64-embedded WAV audio.
- **Features shipped:** 7 waveforms (sine, sawtooth, rsaw, square, triangle, pulse, noise) all non-sine with PolyBLEP antialiasing; ADSR with correct gate-early-close scaling; 4 biquad filter types (LPF/HPF/BPF/notch); per-voice LFO (pitch/filter/amp); 5 drum voices (kick, snare, hihat, open hihat, clap); Freeverb stereo reverb (8 LBCF + 4 allpass per channel); LFO chorus; knee limiter + peak normalizer; 16-step sequencer with polyphony, panning, release tails; procedural composer with 8 musical scales, chord progressions, melody generation, and bass patterns; HTML visualizer (waveform + FFT + piano roll); CLI (`compose/synth/visualize/demo/info`); JSON preset files; `demo.sh`; 84-test suite covering all modules and CLI integration.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review (Phase 3) caught 3 bugs — `soft_limit` formula allowed output 1.89 (corrected to asymptote exactly at 1.0), `compute_adsr` division by zero on empty gate, `parse_note` rejected flat names after `.upper()` mangled the lookup key; Phase-5 testing caught 2 more — `compute_adsr` still reached 1.0 when gate closed during attack (now uses natural duration for scaling), `Delay` buffer was `delay_n+1` so echo arrived one sample late; 84/84 tests green; full demo (4 styles + 6 waveforms) renders without error in ~25s.

## 2026-06-25 — QSim
- **What:** A pure-Python quantum circuit simulator built from scratch — complex state vector (2^n amplitudes, big-endian qubit order), O(2^n) gate application via bitwise amplitude pairing, Born-rule measurement with wavefunction collapse, and nine quantum algorithms from first principles. Includes a Monte Carlo noise model (quantum trajectory method), an ASCII circuit renderer, and a self-contained interactive HTML visualizer with SVG circuits, amplitude histograms, Bloch spheres, and step-through playback.
- **Stack:** Pure Python 3 stdlib only (no NumPy, no scipy, no Qiskit); self-contained HTML/SVG/JS for the visualizer.
- **Features shipped:** 21 standard gates (I, X, Y, Z, H, S, Sdg, T, Tdg, CNOT, CX, CZ, SWAP, iSWAP, Toffoli, Fredkin, + parameterized); Rx(θ), Ry(θ), Rz(θ), Phase(θ), U(θ,φ,λ); `controlled(gate, n_ctrl)` for building n-controlled gates; `apply_1q/2q/3q` O(2^n) implementations; full and partial wavefunction collapse; `sample()` via binary-search CDF; expectation values (X/Y/Z Pauli); entanglement entropy via Jacobi eigenvalue on reduced density matrix; Bloch vector for single-qubit states; 5 noise channels with correct Kraus operators (bit-flip, phase-flip, depolarizing, amplitude damping T1, phase damping T2); `NoiseModel` for per-gate/per-qubit/all-qubit channel application; fluent Circuit builder with 20 gate methods; shot-level noise injection; nine algorithms: Deutsch-Jozsa, Bernstein-Vazirani, Grover (exact iteration formula), QFT, QPE, Teleportation, Simon's period-finding, BB84 QKD, Superdense Coding; ASCII circuit renderer; interactive HTML visualizer; 12-subcommand CLI; 90-test suite.
- **Verdict:** Shipped. 4/4 required (gates, measure, algorithms, noise) + 2 stretch (BB84 QKD + superdense coding); adversarial review (Phase 3) found and fixed 4 bugs — CRITICAL: Grover used `round(π/4·√(N/k))` instead of `floor(π/(4·arcsin(√(k/N))))` causing n=2 to overshoot (25% instead of 100% success), `_rcccx()` built one too few controlled gate layers (used `range(nc-1)` instead of `range(nc)`); MODERATE: Simon accepted all-zeros period string (added validation); MINOR: QPE showed confusing raw vs mod-1 phase display, HTML f-string had unescaped `}` (SyntaxError); Phase 5 testing found Simon silently accepted the all-zeros measurement y='00' as a "sample" (which always satisfies y·s=0 and gives wrong GF(2) null-space vector) — fixed by filtering trivial measurements; 90/90 tests green (gates unitarity, truth tables, Born rule, entanglement entropy, Kraus completeness, statistical accuracy, all 9 algorithms, HTML/ASCII output).

## 2026-06-26 — Flux
- **What:** 2D Navier-Stokes fluid simulator implementing Stam's Stable Fluids (1999) — semi-Lagrangian advection, implicit diffusion, pressure projection — with vorticity confinement (Fedkiw 2001), solid obstacles, multiple dye channels, gravity/buoyancy, a JSON scene format, an interactive single-file HTML playground, and a Python batch renderer/CLI.
- **Stack:** Python 3 + NumPy; hand-rolled PNG encoder/decoder (stdlib `zlib` only); self-contained vanilla JS/Canvas (`flux.html`, zero deps); CLI via `argparse`.
- **Features shipped:** unconditionally-stable NS solver (two-project pipeline, 20-iter Jacobi); vorticity confinement (dimensionless scale 0–10); rectangular + circular solid obstacles enforced every step; 5 colourmap render modes (dye, speed, vorticity, composite, fire); 5 built-in scenes (smoke plumes, ink drops, Kármán vortex street, counter-rotating vortex pair, fire plume); JSON scene format for custom obstacles/timed injections; interactive HTML with mouse/touch drag-inject, right-click obstacles, all parameter sliders, screenshot; CLI (`render/anim/demo/info`); `tests.py` (47 checks); `demo.sh`.
- **Verdict:** Shipped. 47/47 tests green; all 5 scenes stable 200+ steps (no NaN, div_rms < 0.002, obstacle cells exactly 0); ~160 fps at N=64. Adversarial review (Phase 3) caught 5 bugs — vorticity blow-up from spurious N factor in omega/gradient (CRITICAL), obstacle velocity non-zero after final project when vort_scale=0 (CRITICAL), divergence_rms inflated by N factor (MODERATE), inject_dye IndexError on invalid channel (MODERATE), `--mode fire` rejected by CLI (MINOR); Phase 4 fixed a coordinate-axis mismatch (render transpose + scene injection reorientation) so smoke/ink/fire now move in the physically correct direction.

## 2026-06-27 — VecNN
- **What:** A from-scratch vector search engine in pure Python implementing HNSW (Hierarchical Navigable Small World, Malkov & Yashunin 2018) and LSH with multi-probe, three distance metrics, a recall benchmarking harness, JSON persistence, metadata filtering, and a self-contained interactive HTML visualizer.
- **Stack:** Python 3 + NumPy (math/indexing only); no external deps; generated self-contained HTML/JS/CSS for the graph visualizer.
- **Features shipped:** full HNSW (Algorithms 1–5 from the paper: geometric level assignment, beam-search per layer, bidirectional edge linking, diversity heuristic neighbour selection, configurable ef_search); 3 distance metrics each with scalar + vectorised batch variants (cosine/euclidean/inner-product); JSON persistence round-trip for both HNSW and LSH (delete operations persist); exact k-NN oracle + recall@k + QPS sweep benchmarking harness with alignment check; LSH multi-probe (n_probes=1/2/3: exact bucket → single-bit-flips → double-bit-flips, 0.19/0.75/0.99 recall@10); PCA-projected self-contained HTML visualizer (layer selector, animated search walk, pan/zoom, hover tooltips, ~250 KB); metadata filtering (AND-dict post-filter on arbitrary key-value pairs); 7-subcommand CLI (insert/search/bench/compare/viz/info/demo); 85-test suite; demo.sh (D1–D9 end-to-end).
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review (Phase 3) found and fixed 6 issues — LSH serialization bug (bucket keys stored as Python repr strings, broke on reload), ef=0 silently using default (fixed `ef or ef_search` → `ef if ef is not None`), dead variables in bench.py, stats key rename (`total_edges` → `total_edges_directed`), alignment check for benchmark correctness, batch distance for search-layer speed; 85/85 tests green; HNSW recall@10 = 1.000 at ef=40, LSH 3-probe recall@10 = 0.988.

## 2026-06-28 — Optic
- **What:** A complete, dependency-free computer vision library in pure Python 3 (stdlib only — no NumPy, no Pillow, no OpenCV) implementing the classic CV algorithms from scratch: PNG I/O, Gaussian/bilateral filters, Canny edge detection, Harris and FAST corner detectors, binary morphology with union-find connected component labeling, Otsu and adaptive thresholding, Hough line transform, NCC template matching with summed-area tables, and an interactive self-contained HTML pipeline visualizer.
- **Stack:** Pure Python 3 stdlib only (`math`, `zlib`, `struct`, `collections`, `argparse`); generated self-contained HTML/JS for the visualizer with base64-embedded PNGs.
- **Features shipped:** From-scratch PNG reader/writer (IHDR+IDAT+IEND, CRC-32, all 5 filter types, L/RGB/RGBA/LA); Image class with nearest/bilinear resize, arbitrary-angle rotate, flip, crop, blend, normalize; separable 2D Gaussian convolution; Sobel/Prewitt/Scharr gradient operators; full Canny pipeline (blur→gradient→NMS→double-threshold→BFS hysteresis); Harris corners (structure tensor + NMS); FAST corners (16-pixel Bresenham circle + max-arc criterion + NMS); binary dilation/erosion/opening/closing/morph_gradient/top_hat/black_hat/hit_or_miss with cross/square/disk SEs; two-pass connected component labeling with union-find (size, bbox, centroid per component); Otsu thresholding (bin-midpoint fix for binary images); adaptive thresholding via box blur; bilateral filter (σ_s + σ_r, precomputed spatial weights); Hough (ρ,θ) accumulator with peak NMS; NCC template matching with summed-area tables + SSD variant; self-contained HTML visualizer (dark theme, click-to-zoom); 11-subcommand CLI (info/edges/corners/morph/components/hough/match/thresh/denoise/viz/demo); synthetic image generators (checkerboard, shapes, circles, lines); 89-test suite.
- **Verdict:** Shipped. 4/4 required + 4 stretch features; adversarial review (Phase 3) caught 3 bugs — Otsu threshold=0 for binary images caused connected_components to find 1 giant component instead of 3 (fixed with bin-midpoint), cmd_viz wrote `foo_pipeline.html.png` instead of `foo_pipeline.html` (fixed path construction), template_match_ncc silently returned empty response when template > image (added ValueError); FAST early-exit threshold lowered from 3→2 to correctly pass 45° corners; 89/89 tests green in ~1.2s.

## 2026-06-28 — Strata (folder `2026-06-28-kvstore/`)
> Note: this project's own name is "Strata," unrelated to and pre-dating the
> also-named-"Strata" version-control system shipped 2026-07-03 in folder
> `2026-07-03-strata/`. No folder collision (different dates), but readers
> searching by name should check the date.
- **What:** A from-scratch **LSM-tree (Log-Structured Merge-tree) key-value store** in pure Python 3 — the write-optimized storage engine architecture behind LevelDB, RocksDB, Cassandra, HBase, and InfluxDB. Writes land sequentially in a WAL + MemTable; the MemTable flushes to immutable SSTables; leveled compaction merges SSTables into progressively larger levels.
- **Stack:** Pure Python 3 stdlib only (`struct`, `os`, `threading`, `bisect`, `heapq`, `math`, `json`, `http.server`); no external deps.
- **Features shipped:** multi-version MemTable (all versions per key in seq-descending order, enabling snapshot reads); CRC-32–checked append-only WAL with crash recovery on open; from-scratch SSTable format (DataBlocks with seq-number preservation, FilterBlock with Bloom filter, IndexBlock for binary search, 32-byte Footer); from-scratch Bloom filter (FNV-1a + DJB2 double hashing, Kirsch-Mitzenmacher technique, configurable FP rate, `to_bytes`/`from_bytes` with bounds validation); leveled compaction (L0 threshold=4 files → merge all L0 + overlapping L1; LN round-robin → LN+1; tombstone GC only at deepest level; rollback on error); MVCC snapshots (`db.snapshot()` pins sequence number, `db.get(key, snap)` reads at that point); atomic `WriteBatch` under contiguous sequence-number block; thread-safe DB with `threading.Lock`; MANIFEST (JSON-lines append file with fsync); interactive REPL with `snap`/`snapget`/`batch`/`compact` commands; throughput benchmark (ops/sec + MB/sec for seqwrite/randwrite/seqread/randread); HTML level visualizer (`viz` + `serve` subcommands) showing level structure, key ranges, sizes, Bloom filter params, read amplification; 9-subcommand CLI (`repl/get/put/del/scan/compact/info/bench/viz/serve`); 72-test suite + demo.sh.
- **Verdict:** Shipped. 4/4 required + 4/4 stretch; adversarial review (Phase 3) caught 7 bugs — CRITICAL: SSTable records decoded with seq=0 (corrupted compaction MergeIterator dedup — fixed by encoding/decoding seq in DataBlock), scan() materialized results outside lock (race with compaction closing file handles — fixed by materializing inside lock), manifest _append() had no fsync (durability gap — fixed with flush+fsync), tombstone GC was dynamic (could GC tombstones at non-deepest level if files happened to be at max — fixed by binding to Options.max_levels), compaction had no rollback on error (fixed with try/except + manifest remove + file unlink), find_file_for_key returned None on first reader failure instead of continuing binary search (fixed with `continue`), SSTableWriter had dead empty-bloom-filter class (removed); MemTable redesigned from single-version to multi-version after snapshot tests failed; WAL rotation fixed (new WAL was opened in append mode over old content). 72/72 tests green; demo.sh exercises all features end-to-end.

## 2026-06-29 — Morphica
- **What:** A generative algorithmic art engine from scratch — four distinct mathematical families each rendered to SVG/PNG: L-systems (9 presets, stochastic rules, turtle-graphics rasteriser), Gray-Scott reaction-diffusion (6 parameter presets, numpy-accelerated PDE solver, pure-Python fallback), strange attractors (6 kinds — Lorenz, Rössler, Clifford, De Jong, Pickover, Duffing — with log-scaled histogram density colouring), and Voronoi diagrams + weighted stippling (Lloyd relaxation, rejection-sampled point density). Ships a self-contained interactive browser viewer (L-system growth animation, attractor gallery, scrub/play/export), animation CLI for PNG frame sequences (lsystem growth, attractor orbit buildup, RD time-lapse), 12 named colour palettes with palette-blend cycling, and a hand-rolled PNG encoder (Sub filter, zlib, CRC-32).
- **Stack:** Python 3 stdlib + optional numpy (auto-detected, pure-Python fallback for RD); generated self-contained HTML/JS for the viewer.
- **Features shipped:** L-system rewriting engine (deterministic + stochastic) + turtle interpreter (push/pop stack, SVG paths, Bresenham PNG rasteriser) + 9 presets; Gray-Scott RD solver (numpy vectorised explicit Euler + Gauss-Seidel pure-Python fallback, 6 presets, configurable grid/steps); 6 strange attractor orbit generators + flame-algorithm histogram density renderer; brute-force Voronoi rasteriser + Lloyd relaxation centroidal CVT + rejection-sampled weighted stippling; interactive HTML viewer (JS turtle engine, L-system growth animation, canvas export, attractor gallery); `animate` CLI (lsystem/attractor/rd frame sequences); 12-palette gradient system + palette cycling in animation; hand-rolled PNG encoder (verified round-trip); 58-test suite; demo.sh.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review (Phase 3) caught 12 issues — CRITICAL: Duffing attractor OverflowError (wrong map formula, fixed to correct discrete form + divergence guard), PNG channel clamping missing (fixed with & 0xFF mask), 0-point Voronoi guard absent (fixed), voronoi_to_svg generated 14MB rasterised fake SVG (capped at 100×100 grid), RD numpy/pure-Python noise init diverged (unified to single noise-grid pre-computed from shared RNG); all fixed with regressions; 58/58 tests green; demo.sh exercises all 7 subcommands end-to-end.

## 2026-06-30 — Cryptex
- **What:** A from-scratch cryptography workbench implementing AES-256, SHA-256/HMAC-SHA256, RSA-2048, P-256 ECDH/ECDSA, and ChaCha20-Poly1305 in pure Python with no crypto dependencies. Every algorithm is derived from mathematical foundations (GF(2⁸), Fermat's little theorem, elliptic curve point arithmetic) and verified against NIST/RFC test vectors. Includes a self-contained interactive HTML visualizer (AES S-box heatmap, SHA-256 round table, live JS AES-256-CTR) and a full CLI with subcommands for every primitive.
- **Stack:** Pure Python 3 stdlib only (no hashlib, no hmac, no Crypto). OS entropy for key generation only. Generated HTML visualizer is self-contained (no external deps).
- **Features shipped:** GF(2⁸) field (Russian-peasant mul, Fermat inverse, AES affine transform, dynamic S-box); AES-256 Rijndael (SubBytes/ShiftRows/MixColumns/AddRoundKey, 14-round key schedule, ECB/CBC/CTR modes, PKCS#7); SHA-256 (64-round compression from prime fractional parts) + HMAC-SHA256; RSA-2048 (Miller-Rabin primality, ext. GCD, CRT decrypt, OAEP/MGF1, PSS sign/verify, PEM serialization); P-256 ECDH + ECDSA (Jacobian coords, Montgomery ladder, RFC 6979 deterministic k, compressed/uncompressed SEC1 encoding); ChaCha20-Poly1305 AEAD (RFC 8439); HTML visualizer; CLI (aes/sha256/rsa/ecdh/ecdsa/chacha/viz/demo/bench); benchmark subcommand; 95-test suite; 24-check demo.sh.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch (ChaCha20-Poly1305 + HTML viz); adversarial review (Phase 3) found and fixed 10 issues — 3 real algorithm bugs (AES RCON off-by-one, RSA key_bytes truncation, RSA-PSS em_bits mismatch) + 4 critical CLI crashes (raw tracebacks on bad hex/missing args) + 3 UX/minor issues; 95/95 tests green, 24/24 CLI checks pass, 20/20 demo checks pass.

## 2026-07-01 — Ironkey
- **What:** A cryptography suite built entirely from scratch in pure Python — SHA-256, HMAC/PBKDF2, AES-256 (CBC + GCM), RSA (Miller-Rabin keygen + OAEP + PSS), and X25519 ECDH — each verified against official NIST/RFC test vectors and cross-checked against OpenSSL, powering a real password-based encrypted file vault, a forward-secure two-party messaging session, and a working implementation of Vaudenay's padding-oracle attack.
- **Stack:** Pure Python 3 stdlib only (no `hashlib`/`hmac`/`Crypto`/`cryptography`/`ssl` in any crypto math — just `os.urandom` for CSPRNG bytes); `openssl`/`gcc` used only as an independent test-time oracle (skipped gracefully if absent); self-contained HTML/CSS/JS for the visualizer, no CDN/deps.
- **Features shipped:** SHA-256 (FIPS 180-4, bit-exact vs `hashlib` incl. 1M-`"a"` and boundary lengths); HMAC-SHA256 (RFC 4231) + PBKDF2-HMAC-SHA256, with a schedule-caching optimization for PBKDF2's repeated-key inner loop; AES-128/192/256 (FIPS 197, S-box generated from GF(2^8) inversion + affine transform, not hardcoded) with CBC and GCM (Galois/Counter Mode w/ from-scratch GHASH), cross-checked against OpenSSL for both modes; RSA keygen (hand-written Miller-Rabin + CRT), OAEP encryption and PSS signatures (RFC 8017); X25519 (RFC 7748 Montgomery ladder), cross-checked against OpenSSL-derived keypairs both directions; ephemeral-X25519 + AES-GCM messaging session with tamper + replay detection; a password-based file vault (PBKDF2 + AES-256-GCM, self-describing container); an interactive HTML visualizer animating real AES round-by-round state and SHA-256 compression from actual execution traces; a full from-scratch Vaudenay padding-oracle attack that recovers plaintext with zero key access from a deliberately unauthenticated CBC box, then correctly fails against the real GCM vault; an 11-subcommand CLI (`hash/hmac/kdf/aes/rsa/dh/session/vault/attack/viz/demo`); 122-test suite + demo.sh (16-check CLI walkthrough).
- **Verdict:** Shipped. 4/4 required + 3/3 stretch (plus the vault promised in the plan); adversarial review found a CRITICAL performance bug (AES re-derived its full round-key schedule from scratch on every 16-byte block — a 1 MB GCM encrypt took 43s — fixed via per-key schedule caching + lookup-table MixColumns/SubBytes, down to ~5.5s/MB), an OAEP decryption oracle (an out-of-range ciphertext raised a distinguishable error message from a bad-padding one, the exact Manger's-attack shape RFC 8017 warns against — unified to one generic message), dead leftover-refactor code in `pss_verify`, and a real non-uniformity bug in the hand-written random-integer sampler (proven by exact enumeration, not sampling) — all fixed with regressions; PBKDF2 throughput (~2,800 iter/s) meant the OWASP-recommended 600,000 iterations would take ~3.5 minutes here, so `vault.py` defaults to a measured, honestly-disclosed 4,096 (~1.5s, bcrypt-like) instead of cargo-culting a number calibrated for compiled implementations; 122/122 tests green, all documented commands hand-verified end to end.

## 2026-07-02 — Graft
- **What:** A version control system built from scratch in pure Python — content-addressable object store (blob/tree/commit, SHA-1 + zlib), a staging area, commit DAG with branches and detached HEAD, a from-scratch Myers O(ND) diff engine, three-way merge with conflict markers, and a custom packfile format with delta compression. Uniquely verifiable: real `git` is installed on the box, so object encoding was differentially checked against it rather than only self-consistency.
- **Stack:** Pure Python 3 stdlib only (no `dulwich`/`pygit2`/GitPython, no shelling out to git for any actual VCS logic); real `git` used solely as an external test oracle.
- **Features shipped:** loose-object store byte-identical to git (verified: real `git cat-file` reads Graft's raw objects directly, `git write-tree` matches Graft's tree SHA on an identical layout); staging area + `add/rm/status/commit` building real tree objects, resolving pathspecs relative to cwd from any subdirectory; `log/branch/checkout` with parent-chain traversal and safe checkout that refuses to clobber uncommitted work; Myers diff → git-style unified diff (context-trimmed hunks) with binary-file detection; three-way merge (BFS merge-base, fast-forward detection, line-level conflict markers, `MERGE_HEAD` so post-conflict commits get two parents) matching real git's "touching hunks conflict, hunks separated by an unchanged line merge cleanly" semantics; `graft gc` packing loose objects into a custom pack with copy/insert delta compression, self-verifying every object before deleting loose copies; interactive HTML commit-graph + diff visualizer (`graft viz`); 12-command CLI; 71-test suite (unit, a 3,000-trial Myers-diff fuzz vs. a brute-force DP oracle, a 1,000-trial merge fuzz vs. real `git merge-file`, packfile round-trips, 20 CLI end-to-end tests) + `demo.sh`.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review found and fixed 6 issues — worst was a three-way-merge conflict-detection bug found only by fuzzing against real `git merge-file`: a hand-written unit test had encoded a wrong assumption (that non-overlapping-but-adjacent edits merge cleanly), when real git actually conflicts on *touching* hunks and only merges cleanly with an unchanged line of separation; also fixed `add`/`rm` breaking when run from a subdirectory, a second-order false-conflict bug from the first merge fix, garbled binary diffs, an unhandled `BrokenPipeError`, and a demo-script `pipefail` bug that made a correct feature look broken. 71/71 tests green, including 4,000+ combined fuzz trials against independent/real-git oracles with zero mismatches.

## 2026-07-03 — Strata (folder `2026-07-03-strata/`)
> Note: unrelated to the also-named-"Strata" LSM-tree key-value store shipped
> 2026-06-28 (folder `2026-06-28-kvstore/`) — pure name coincidence. This
> Strata is also, coincidentally, a close conceptual sibling of Graft
> (2026-07-02) and Palimpsest (2026-07-04): three from-scratch Git-like VCS
> implementations shipped on three consecutive days, each built without
> visibility into the others (none were merged to `main` — and thus into any
> other session's `LEDGER.md` view — until all were merged together on
> 2026-07-04). See Palimpsest's entry for the full note on this collision.
- **What:** A version control system built from scratch in pure Python — a real Git-like VCS, not a toy: a content-addressable object store (SHA-256, zlib, integrity-checked reads), a proper Myers O(ND) diff engine, merge-base discovery over an arbitrary commit DAG, a three-way (diff3-style) merge with real conflict markers, and an interactive self-contained HTML commit-graph visualizer.
- **Stack:** Pure Python 3 stdlib only (no deps — `hashlib`, `zlib`, `json`, `argparse`); self-contained HTML/SVG/JS for the visualizer (no server, no build step).
- **Features shipped:** content-addressable object store (blob/tree/commit, git-style `"type len\0content"` framing, atomic writes, integrity check on every read); staging + commit workflow (`add` incl. staged deletions, `status`, `commit`, `log`, deterministic tree encoding independent of insertion order); branching & checkout (detached HEAD, safety checks that refuse to clobber uncommitted *or* untracked work, empty-directory pruning); Myers O(ND) diff with unified-diff rendering verified byte-for-byte identical to `difflib.unified_diff` across 2,000+ fuzz cases, and verified truly minimal against a brute-force LCS oracle across 12,000+ cases; merge-base discovery (ancestor-set BFS over a multi-parent DAG); three-way merge via a two-pointer synchronized opcode walk producing real conflict markers, with `MERGE_HEAD` tracking so resolved conflicts produce proper 2-parent merge commits; interactive HTML commit-graph visualizer (SVG DAG with per-branch lanes, click-to-inspect commits, per-commit Myers diffs, dark/light aware, verified in headless Chromium); `strata config` (user.name/email); full CLI (`init/add/status/commit/log/branch/checkout/diff/merge/viz/show/config`); 98-test suite + 9-step demo.sh.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch. Adversarial review (Phase 3) found 15 real issues by hostile testing before shipping — including a Myers-diff oracle that was itself wrong (difflib is heuristic, not minimal — replaced with a true DP oracle), a 3-way merge that silently dropped conflicting EOF insertions (rewritten as a two-pointer opcode walk, re-verified with 1,000+ property-based fuzz cases), merge commits losing their second parent, and **two real path-traversal vulnerabilities** (unsanitized branch names, tree-entry names missing `/` validation on the read path) — all fixed and re-verified; also fixed raw-traceback-on-corrupt-object (a failure mode a previous ledger entry called out as its worst finding, checked for here and caught). 98/98 tests green; demo.sh's 9 steps (object store → add/commit/log → branch/checkout → diff → fast-forward merge → conflicting 3-way merge → staged deletion → untracked-file safety → visualizer) all pass end-to-end.

## 2026-07-04 — Palimpsest
> Note: built without visibility into Graft (2026-07-02) or Strata
> (2026-07-03) — neither branch was merged into `main` (and thus into this
> session's `LEDGER.md`) until all twelve outstanding branches, including
> this one, were merged together after the fact. All three are independent
> from-scratch Git-like VCS implementations (object store, Myers diff,
> three-way merge, HTML commit-graph viz) built on three consecutive days —
> a genuine, only-visible-in-hindsight collision, not a rules violation.
- **What:** A version control system built from scratch in pure Python — Git's exact object model (content-addressable blob/tree/commit objects, SHA-1 addressed, zlib-compressed), a staging area, branches/HEAD/checkout, a real Myers O(ND) diff engine, three-way (diff3-style) merge with LCA merge-base search, and an interactive self-contained HTML commit-graph + diff visualizer. No runtime dependency on the real `git` binary — it's used only as a test oracle.
- **Stack:** Pure Python 3 stdlib only (hashlib, zlib, json, argparse; no deps); generated self-contained HTML/CSS/JS for the visualizer; real `git` binary and `patch(1)` used exclusively in tests as verification oracles.
- **Features shipped:** blob/tree/commit encoding byte-identical to real Git (differentially verified against `git hash-object`/`git mktree`/`git commit-tree`, including the "directory mode is `40000` not `040000`" gotcha caught mid-build); staging area with `add`/`status`/`commit` that correctly stages file deletions and stores symlinks as their link-target text instead of dereferenced content; branches/HEAD/`checkout`/`switch`/`log` with detached-HEAD-by-sha-prefix support; Myers diff engine (verified minimal against a brute-force LCS oracle over 2000 cases, and its unified-diff output verified byte-correct by feeding 500 generated diffs through the real `patch` command) with binary-file detection; three-way merge (merge-base checked against `git merge-base`, fast-forward detection, clean auto-merge of non-conflicting edits, real conflict markers otherwise); interactive HTML commit-graph visualizer (branch chips, click-through unified diff per commit, dark-mode aware, screenshot-verified in headless Chromium).
- **Verdict:** Shipped. 4/4 required + 2/2 stretch; adversarial review found 10 issues across phases 3–4 — worst were two critical bugs (file deletions could never be staged/committed at all, and symlinks were silently corrupted into the dereferenced content of the file they pointed at instead of the link-target string) plus a real HTML/script-injection bug in the visualizer (an attacker-controlled commit message containing `</script>` could break out of the embedded JSON's script block) — all fixed with regression tests; 92/92 tests green plus a 19-check `demo.sh` exercising every feature through the real CLI.

## 2026-07-06 — Formulate
- **What:** A spreadsheet built from scratch — lexer → recursive-descent parser → AST → evaluator formula language (real Excel-style operator precedence, relative/absolute/cross-sheet references, 30+ built-in functions), a live precedents/dependents dependency graph with dirty-propagation incremental recalculation, and an interactive browser UI backed by a real Python `http.server` engine (the browser holds zero formula logic — every edit is a network round trip, same pattern as Gambit's server-backed chess board).
- **Stack:** Pure Python 3 stdlib only (no deps) for the entire engine + server; a single static HTML/CSS/JS page for the UI with no client-side formula evaluation; Playwright/Chromium (pre-installed, driven via Node) for a headless-browser UI smoke test.
- **Features shipped:** full formula engine (lexer/parser/evaluator, `%`>`^`>unary-`>`*/`>`+-`>`&`>comparisons precedence, `A1`/`$A$1`/`A1:B10`/`Sheet2!A1` refs, 30+ functions incl. `SUMIF/COUNTIF/AVERAGEIF/VLOOKUP/INDEX/MATCH`, Excel-style errors `#DIV/0! #VALUE! #NAME? #REF! #N/A #NUM! #CIRC!`); incremental dependency-graph engine (Kahn's-algorithm dirty-set recalculation, circular-reference detection, fuzz-verified equivalent to full recompute over randomized edit sequences); interactive spreadsheet UI (grid, full keyboard nav, formula bar, multi-level undo/redo, inline error highlighting, empty-workbook hint); JSON workbook save/load + CSV import/export; **all 3 stretch features shipped**: autofill/drag-fill with relative-reference shifting (absolute refs pinned), live in-browser bar/line charts that redraw on recalculation, and multi-sheet workbooks with cross-sheet references (incl. forward references to sheets that don't exist yet); `formulate` CLI (`serve/demo/export/test`); `demo.sh` running the full verification suite.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review (Phase 3) found and fixed 9 real bugs — worst was a CRITICAL crash where non-finite/complex arithmetic (`=1e400`, `=POWER(-8,0.5)`, `=POWER(10,400)`) escaped evaluation entirely and could crash display formatting or an entire batch edit (fixed with a finite/complex guard at the single evaluation choke point plus a broad exception net around the function library), a correctness bug where cross-sheet references broke for almost any real sheet name because the lexer force-uppercased sheet names along with function names (fixed by preserving case and uppercasing only at function dispatch), a related ref-vs-name lexer ambiguity for sheet/function names ending in digits, a backwards unparse-precedence table that corrupted autofilled formula text across unary-minus-over-power expressions, and two DOM-event-ordering bugs in the frontend (duplicated first keystroke; double-fired Tab/Enter) caught only by the headless-browser smoke test, not the Python suite — all fixed with regression tests; 82/82 Python tests green (incl. a randomized fuzz oracle proving incremental recalculation always matches a full recompute), an 11-check end-to-end API walkthrough, and a headless-Chromium smoke test driving the real UI (type, drag-fill, force a circular reference, add a sheet) all pass.

## 2026-07-05 — Sift
- **What:** A full-text search engine built entirely from scratch in Python — the classic lexical-search half of information retrieval (as opposed to VecNN's embedding/vector search, 2026-06-27): tokenizer, a real from-scratch Porter stemmer, an inverted index, a boolean/phrase/wildcard/fuzzy query engine, BM25 ranking, a BK-tree for typo tolerance, a custom on-disk binary index format ("mini Lucene segment"), and an interactive HTML search UI.
- **Stack:** Pure Python 3 stdlib only (no deps, no NLTK/whoosh/elasticsearch); self-contained HTML/CSS/JS for the search UI served by stdlib `http.server`; Playwright/Chromium used only to visually verify the UI in a real browser.
- **Features shipped:** unicode-aware tokenizer + stopword filter + a complete 5-step Porter stemmer (all suffix-stripping rules, not a stub) verified against 75 of Porter's own reference vocabulary pairs; inverted index (term → doc id/tf/positions) with corpus stats for ranking; recursive-descent query parser for `AND`/`OR`/`NOT` with parenthesized grouping and `"exact phrase"` queries resolved via true positional adjacency (not substring search); Okapi BM25 ranking verified term-by-term against an independent brute-force scorer that re-tokenizes the raw corpus from scratch (exact agreement across many queries/docs/30 random term subsets); a BK-tree over the vocabulary with real Levenshtein range queries powering `term~N` fuzzy search and "did you mean" suggestions, verified against a naive O(n·m) edit-distance oracle; a prefix trie for `term*` wildcard queries; best-window snippet extraction with `<mark>` highlighting; a custom on-disk binary index format (varint delta-encoded postings) that round-trips vocabulary/postings/query-results exactly; an interactive dark-mode-aware HTML search UI backed by a real `http.server` JSON API, screenshot-verified in headless Chromium; a 40-document self-authored synthetic test corpus across 10 topics; full CLI (`index/search/serve/demo`).
- **Verdict:** Shipped. 4/4 required + 4/4 stretch; adversarial review (Phase 3) found and fixed 4 real bugs — an unterminated phrase quote (`"unclosed phrase`) silently truncated the last letter of the query instead of erroring, a misplaced wildcard `*` (`abc*def`) silently searched a dead literal term that could never match instead of erroring, boolean-operator keywords (`AND`/`OR`/`NOT`) leaked into "did you mean" suggestions and crowded out the actually-useful correction, and a document whose first line was blank got an empty display title — all fixed with regression tests; Phase 4 polish caught and fixed a title-duplicated-in-snippet UX issue via a real-browser screenshot pass; 123/123 tests green plus a 30-check `demo.sh` exercising every feature through the real CLI and a live HTTP server.

## 2026-07-07 — Loom
- **What:** A tiny LLM built entirely from scratch — a hand-written reverse-mode autograd engine over NumPy arrays (no PyTorch/TensorFlow/JAX/`transformers`/any autograd library — NumPy is used only as an array/BLAS substrate), a byte-level BPE tokenizer, a GPT-style decoder-only Transformer built as pure compositions of the autograd engine's own ops, and a real Adam-optimized training loop — trained end to end on Tiny Shakespeare (~1.1MB, public domain): val loss 6.27 → 3.12, val perplexity 529 → 22.6 over 2000 steps, producing real learned structure (speaker-header formatting, capitalization, word spacing, archaic pronouns) that an untrained model sampled the same way (byte garbage, no word boundaries) doesn't have.
- **Stack:** Pure Python 3 + NumPy only (no ML framework of any kind — the entire forward *and* backward pass of every layer, including the Transformer, is hand-derived and traced by a custom computational graph); self-contained interactive HTML/SVG/JS visualizer (validated categorical/sequential palette, dark-mode aware, no deps); Playwright/Chromium (pre-installed, driven via Node) for a headless-browser smoke test of the visualizer.
- **Features shipped:** reverse-mode autograd engine (`Tensor`: add/mul/matmul/pow/exp/log/tanh/sum/mean/reshape/transpose/getitem/cat, all broadcasting-aware, plus a `no_grad()` inference mode) verified against central-difference numerical gradients on every op; byte-level BPE tokenizer trained from scratch (frequency-ranked pair merging) with exact round-trip on arbitrary Unicode/punctuation text, no `<unk>` token possible; GPT-style Transformer (causal multi-head self-attention, pre-norm LayerNorm, GELU MLP, weight-tied output head) built entirely from the autograd engine's own ops so its backward pass is 100% engine-derived; from-scratch Adam (bias-corrected moments) + linear-warmup/cosine LR schedule + gradient clipping; **both stretch features shipped**: sampling strategies (greedy/temperature/top-k/top-p nucleus) plus a `loom chat` REPL, and an interactive HTML visualizer (per-head attention heatmaps on a live prompt, the real training loss/perplexity curve, and a from-scratch power-iteration PCA projection of the embedding table — no `np.linalg.eig`/`svd`); `loom` CLI (`tokenizer train/encode/decode`, `train`, `generate`, `chat`, `viz`); `demo.sh` running the full test suite plus a live walkthrough of every feature against the real checkpoint.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch. Adversarial review (Phase 3, 8 independent review passes) found and fixed 8 real bugs — an off-by-one in `Dataset.get_batch` that crashed on minimum-length corpus splits (and silently excluded the last valid window otherwise), a `top_k=0`/negative-`top_k` that silently filtered nothing instead of erroring (Python's `-0 == 0`), a wrong-inverse-permutation bug in `Tensor.transpose` for axis lists mixing negative and positive indices, a raw `KeyError` from out-of-range tokenizer ids reachable via the CLI, missing checkpoint shape/vocab-size cross-validation that deferred real config mismatches into confusing downstream tracebacks, and a visualizer crash on a valid-but-degenerate 0-layer model — plus a deeper architectural fix beyond just patching symptoms: every autograd op's backward closure captured itself in a reference cycle, so `train.py`/`generate.py` had been patched with ad-hoc `gc.collect()` calls; added a proper `no_grad()` inference mode instead (4x less peak memory, ~20% faster generation, byte-for-byte identical output) so pure-inference forward passes never build a cyclic graph in the first place. 67/67 tests green (was 44 pre-review; +19 regression tests, one per confirmed bug, +4 CLI error-path tests in Phase 4), `demo.sh` green end to end on the first run, and the visualizer re-verified in a real headless Chromium run with zero console errors on both the normal checkpoint and the new 0-layer edge case.

## 2026-07-08 — Loom
- **What:** A GPT-style decoder-only Transformer language model built entirely from scratch — its own reverse-mode autograd tensor engine (NumPy used only as a dense-array/BLAS backend; no PyTorch/TensorFlow/JAX/scikit-learn anywhere), its own byte-level BPE tokenizer trained from real corpus statistics, multi-head causal self-attention with an optional KV-cache, a real Adam training loop, and an interactive HTML visualizer replaying the real attention weights of the real trained model as it generates text. No prior daily build had touched the architecture behind modern LLMs.
- **Stack:** Pure Python 3 + NumPy (math/BLAS backend only — every gradient rule, every module, the tokenizer, optimizer, and training loop are hand-written, no ML framework of any kind); generated self-contained HTML/JS for the attention visualizer.
- **Features shipped:** from-scratch autograd `Tensor` engine (add/sub/mul/div/matmul/transpose/reshape/sum/mean/exp/log/sqrt/pow/tanh/relu/softmax/embedding/cross_entropy, broadcasting-aware), every op verified against numerical finite-difference gradients; GPT architecture (token+positional embeddings, multi-head causal self-attention, pre-norm residual blocks, GELU MLP, weight-tied output head) with a hard-verified causality invariant (perturbing a future token never changes any earlier position's output) and a KV-cache verified numerically identical to the non-cached path; byte-level BPE tokenizer trained from corpus statistics (not hardcoded merges), exact round-trip on arbitrary unicode/emoji never seen during training; Adam optimizer (bias-corrected) + LR warmup/cosine-decay + global-norm gradient clipping; a real training run (2,500 steps, ~21.5 min CPU) that took loss from 5.93 → 0.31 train / 0.26 val on an original ~85 KB corpus of template-generated forest fables (not scraped or copied), producing grammatical, on-theme, non-verbatim generated text; interactive attention-heatmap HTML visualizer replaying a real captured generation per-layer/per-head; interactive streaming `chat` REPL with a correct incremental-UTF-8 decoder (a naive per-token decode can split a multi-byte character across two tokens and garble it); full CLI (`train/generate/chat/tokenize/gradcheck/viz/demo`); 70-test suite + `demo.sh`.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review (Phase 3) caught a CRITICAL reference-cycle memory blow-up in the autograd engine (every op's backward closure captures its own output tensor, a genuine cycle that Python's refcounter can't free — a benchmark run was OOM-killed after only a few training steps; fixed by explicitly breaking closures/parent-links on every non-leaf node right after `backward()`), then a second bug in that very fix (the cleanup also nulled leaf/parameter tensors' harmless default backward, crashing the *next* training step — caught by a regression test before it ever hit a real run), plus a tokenizer-decode crash on undertrained/sampled token sequences, a streaming-chat multi-byte-character garble, a duplicated-logic monkey-patch in the visualizer (refactored to a clean side-effect instead), dead code, and a silent no-op on an over-long KV-cached generation request (now warns) — all fixed with regressions; 70/70 tests green, `demo.sh` end-to-end green, and a real checkpoint (381,792 params) is checked in along with a sample attention-visualizer HTML rendered from it.

## 2026-07-09 — Loom
- **What:** A from-scratch tensor autograd engine — a `Tensor` class over NumPy arrays with a dynamic computation graph and broadcasting-aware reverse-mode backward for every op (add/mul/matmul incl. batched-broadcast, pow via exponentiation-by-squaring, exp/log/sqrt/tanh/relu, reshape/transpose, fancy indexing, cat, softmax/log_softmax, masked_fill) — powering a GPT-style decoder-only Transformer (causal multi-head self-attention, GELU MLP, pre-LN residual blocks, weight-tied output head) built entirely out of `Tensor` ops, trained with a hand-coded Adam optimizer on a bundled original text corpus, and sampled from with temperature/top-k/nucleus decoding — the whole loop from "here is a matrix" to "here is a model that writes sentences it was never shown," with zero ML-framework dependency and every gradient finite-difference checked.
- **Stack:** Pure Python 3 + NumPy (raw array/BLAS math only — no autograd, no `nn`, no optimizer anywhere but this codebase); self-contained HTML/Canvas/JS for the attention visualizer (data embedded inline, no server/fetch); Playwright/Chromium for a headless UI smoke test only.
- **Features shipped:** `Tensor` autograd engine with correct broadcasting-undo on every backward pass, gradient-checked against finite differences for all 23 primitive ops plus a full GPT forward→loss→backward pass (168 sampled parameter entries, max relative error ~6e-6); GPT-style Transformer (token+positional embeddings, causal multi-head attention w/ verified-zero future-position weights, GELU MLP, pre-LN residuals, weight tying); from-scratch Adam (bias-corrected moments, grad-norm clipping, LR warmup+cosine decay) with bit-identical checkpoint round-tripping; char-level tokenizer + batching over two original bundled corpora (no scraped data); autoregressive generation (temperature/top-k/nucleus) + interactive CLI chat; interactive attention visualizer (per-layer/per-head heatmaps with hover tooltips over real tokens, real train/val loss curve, self-contained no-server HTML); configurable `quickstart/tiny/small` model presets; full CLI (`gradcheck/info/train/generate/chat/viz/demo`); 43-test suite + `demo.sh` running the whole pipeline live.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch; adversarial review (Phase 3) caught a CRITICAL performance bug — NumPy's `**` operator fell back to a >50x-slower generic path for integer exponents outside `{-1,0,1,2}` on this platform, and GELU's cube term made every MLP forward the dominant training cost (a 1500-step run would've taken ~25 minutes) — fixed with a hand-written exponentiation-by-squaring `_pow`, ~6x faster end to end with zero change to gradcheck results; also caught a CRITICAL crash where `Dataset`'s train/val split clamp could silently hand out a validation split shorter than one block whenever `block_size+1 <= len(corpus) < 2*(block_size+1)`, crashing deep in `numpy.random.randint` with an opaque `high <= 0` (fixed with an upfront precondition check and a clear error message), plus raw tracebacks on common CLI mistakes (missing checkpoint, out-of-vocabulary prompt char, empty viz prompt) now caught and reported cleanly — all fixed with regression tests; 43/43 tests green (incl. finite-difference gradcheck of every op, causal-mask correctness, checkpoint bit-identity, and the exact Phase-3 crash reproduced and now failing cleanly), and a real 800-step training run on the bundled corpus (train loss 4.03→1.25) with its checkpoint and a live attention-visualizer export checked in as working examples.

## 2026-07-10 — Faraday
- **What:** A SPICE-like analog circuit simulator built entirely from scratch — real Modified Nodal Analysis (MNA), a hand-rolled generic LU solver (works over `float` and `complex` unmodified), Newton-Raphson nonlinearity for diodes, backward-Euler transient companion models, and complex-phasor AC small-signal analysis — the same core algorithm every SPICE-family simulator (ngspice/LTspice/PSpice) is built on. A genuinely new domain for this repo: no prior build had touched analog circuit/numerical-ODE simulation, and it comes with exceptionally strong closed-form ground truth (RC time constants, LC resonant frequency, op-amp gain formulas) instead of "looks plausible."
- **Stack:** Pure Python 3 stdlib only (no numpy — the LU solver is hand-written Gaussian elimination with partial pivoting); stdlib `http.server` for a real interactive browser UI (server-backed, zero client-side circuit math, same pattern as this repo's Gambit/Formulate); Playwright/Chromium (pre-installed) for a headless-browser UI smoke test.
- **Features shipped:** MNA engine + DC operating-point solver (node/branch unknown numbering, per-element matrix stamping) verified against hand-solved voltage dividers and KCL on a 5-resistor mesh; transient analysis via backward-Euler companion models verified against the closed-form RC charge and RL current-rise exponentials; nonlinear Shockley-equation diode via Newton-Raphson (SPICE-style critical-voltage damping + GMIN regularization) verified with a half-wave rectifier; interactive server-backed circuit builder + oscilloscope/Bode UI (component-table and netlist-text editors, live node-rail schematic view, real DC/transient/AC charts); **both stretch features shipped**: AC small-signal Bode sweep verified against the analytic RC cutoff and RLC resonant frequency, and an ideal op-amp (nullor/virtual-short MNA stamp) verified against the textbook `-Rf/Rin`/`1+Rf/Rin` gain formulas; plus a SPICE-like netlist format (engineering suffixes, PULSE/SIN sources, import/export) and a 9-circuit preset library; `faraday` CLI (`dc/tran/ac/netlist/list-presets/serve/demo`); 48-test suite + `demo.sh`.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch; adversarial review (Phase 3) found and fixed 8 issues — worst were a bug that broke the AC/Bode view for *every* circuit (ground's magnitude-in-dB is `log10(0)=-inf`, and Python's `json.dumps` happily emits the illegal `-Infinity` token that crashes a browser's `JSON.parse`) and a lossy netlist export that silently dropped a `PULSE`/`SIN` source's waveform down to its DC value, breaking the rectifier demo preset with no error at all — plus reverse-bias diode conductance underflowing to exact `0.0` well within ordinary operating voltages (fixed with SPICE's standard GMIN regularization) and 4 smaller issues; Phase 5 verification caught one more real bug via `demo.sh` actually writing and measuring a full ~2000-row transient CSV instead of spot-checking a few rows: `--uic` transient runs leaked a phantom one-sample-long current series for any capacitor-bearing circuit, crashing the CSV writer with an `IndexError` on any row past the first. All fixed with regression tests; 48/48 unit tests green (incl. 30 randomized linear-algebra systems, HTTP-server route tests against a real running server, and full-preset netlist round-trips), `demo.sh` green end-to-end, and the UI driven in real headless Chromium (presets, all 3 analyses, manual add/delete, error paths) with zero console errors.

## 2026-07-11 — Loom
- **What:** A Transformer language model built on a from-scratch reverse-mode tensor autodiff engine — the primitive set real deep learning frameworks run on (batched/broadcasting matmul, softmax, LayerNorm, GELU, embedding gather, causal masking, fused cross-entropy), each operator's hand-derived backward pass proven correct against finite-difference gradients rather than trusted on faith. A GPT-style decoder-only Transformer (multi-head causal self-attention, pre-LN residual blocks, tied output head) is built purely from those ops, so its backward pass requires zero model-specific backward code — it falls out of composition, and the full model gradient-checks end-to-end across all parameter tensors with no special-casing. The first deep-learning-adjacent build in this ledger to go past scalar autodiff (Cotangent, 2026-06-16) into tensors and attention.
- **Stack:** Pure Python 3 + `numpy` used strictly as a fast ndarray container and BLAS matmul (no PyTorch/JAX/TensorFlow/autograd anywhere); self-contained HTML/CSS/JS for the visualizer; Playwright/Chromium (pre-installed) for a headless-browser UI smoke test.
- **Features shipped:** tensor autodiff engine (`tensor.py`) with broadcasting-aware matmul/arithmetic/reductions, softmax, LayerNorm, GELU, embedding gather, causal masking, fused cross-entropy — every op finite-difference gradient-checked to ~1e-9; GPT-style Transformer (`nn.py`) built entirely from those ops; byte-level BPE tokenizer trained from scratch with exact round-trip on unicode and the full 256-byte range; from-scratch Adam optimizer + minibatched training loop with gradient clipping and checkpointing, loss provably decreasing (asserted in tests); **all 3 stretch features shipped**: temperature/top-k/top-p sampling, an interactive HTML visualizer (real per-layer/per-head attention heatmaps, scrubbable/playable generation replay, real loss curve, dataviz-skill-validated palette, light+dark mode, browser-verified zero console errors), and a one-command `demo` that trains a real tokenizer+model on a bundled original short story in ~17s CPU and prints an objective untrained-vs-trained comparison (~6.2 → ~3.5 nats/token); full `loom` CLI (`train/generate/tokenize/gradcheck/viz/demo`); `demo.sh` exercising every feature through the real CLI.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review (Phase 3) found and fixed 7 real issues — worst was a genuine crash where a validation split smaller than `block_size` could form on modest corpora and kill training mid-run partway through (reproduced directly, fixed by guaranteeing a minimum val-split size with graceful fallback for tiny corpora), plus dead code (`_merge_rank` written but never read), `assert`-based validation that `python -O` would silently strip, a code-duplication hazard between the training and attention-visualization forward paths (unified into one path, parity-verified), and a nucleus-sampling float-precision edge case; Phase 4 polish caught one more real bug (`generate(prompt='', max_new_tokens=0)` emitted one unrequested random character instead of `''`) plus a latent numpy-int64/JSON-serialization landmine in the attention-viz trace, both fixed with regression tests. A test-writing session also surfaced a genuine (non-bug) mathematical fact worth recording: with weight-tying and a zero-mean final LayerNorm, a perfectly *uniform* shift to every embedding row is provably invisible in the output logits — caught when a naive test assertion failed for the right reason. 84/84 tests green (finite-difference gradchecks on every tensor op, a full end-to-end GPT gradient check across 28 parameters, a functional causal-masking correctness test, tokenizer round-trips, Adam convergence, the val-split crash regression, sampling edge cases, checkpoint round-trip fidelity, a full pipeline smoke test, and a headless-Chromium test exercising the real visualizer's controls); 8/8 `demo.sh` checks pass end-to-end.

## 2026-07-12 — Kinesis
- **What:** Evolving virtual creatures from scratch, in the spirit of Karl Sims' 1994 "Evolving Virtual Creatures" — a GP-style recursive limb-growth genome (body plan) plus per-joint CPG motor parameters, expanded into a phenotype tree of rigid segments; a from-scratch 2D constraint-based rigid-body physics engine (sequential-impulse solver, motorized/limited revolute joints, ground contact + friction); and a genetic algorithm (tournament selection, elitism, subtree-graft crossover, mutation) that selects purely for horizontal distance traveled. Body plan and controller co-evolve together, so whatever limb counts, shapes, and gaits win are an output of the run, not chosen in advance.
- **Stack:** Pure Python 3 stdlib only (no NumPy, no Box2D/PyMunk/pybullet, no ML framework — the physics engine, GA, and CPG controllers are all hand-written); self-contained HTML/Canvas/SVG for the replay viewer, population gallery, and fitness chart (no CDN, no build step); Playwright/Chromium (pre-installed) used only to visually verify the generated HTML in headless Chromium.
- **Features shipped:** recursive limb-growth genome → phenotype generator (mirrored limb pairs, bounded depth/segment-count, deterministic forward-kinematics placement so joint anchors exactly coincide at construction); from-scratch 2D rigid body engine (semi-implicit Euler, Gauss-Seidel sequential impulses, revolute joint = point-to-point constraint + PD-servo motor + one-sided angle limit, ground contact with Coulomb friction, Baumgarte stabilization) verified against analytic free-fall and physical-pendulum-period formulas; per-joint CPG controllers (amplitude/frequency/phase/offset genes) coupled to whatever body plan the genome encodes; GA with tournament selection + elitism + subtree-graft crossover + structural/parametric mutation, proven to measurably improve fitness from a random-genome baseline; **all 3 stretch features shipped**: animated self-contained HTML replay viewer (camera-follow, scrub/play), population gallery HTML (whole generation, sorted by fitness, rest-pose silhouettes), and real checkpoint `evolve --resume` (continues a saved run without re-simulating anything already evaluated); 6-subcommand CLI (`evolve/replay/gallery/fitness-chart/demo/test`); 49-test suite + `demo.sh` (9 feature-exercising steps + the full test suite).
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review found and fixed 10 real bugs — 2 CRITICAL physics bugs that looked like "creative" evolved behavior but weren't (an unbounded Baumgarte position-correction bias let one bad frame explode into an unphysical 10-rotation, 28 m/s launch, fixed with a correction clamp à la Box2D's `b2_maxLinearCorrection`; a fixed global motor-torque constant gave small limbs an effectively enormous angular acceleration, letting evolution "win" via thrashing instead of walking, fixed by scaling torque to each joint's own moment of inertia), 2 genome-invariant bugs (a depth-repair pass that picked "the node with the largest local subtree height" to prune — always the root, since subtree height strictly decreases going down any path — so it silently never trimmed anything; a `_grow` node-count check scoped to the wrong subtree, letting even a freshly-random genome exceed the segment cap, caught by the plainest possible test) plus a NaN-propagation correctness bug with no crash and no obvious symptom (`max(..., key=...)` treats every NaN comparison as `False`, so a NaN fitness landing first in iteration order could silently corrupt GA selection) and 5 CLI/API robustness issues (raw tracebacks on bad input, a crash that would discard a long evolution run because the output directory didn't exist, a duplicate-history-entry bug found while building the resume feature itself); 49/49 tests green (incl. a regression test per bug found) and `demo.sh`'s full 9-step walkthrough + test suite pass end-to-end.

## 2026-07-13 — Kiln
- **What:** A from-scratch WebAssembly toolchain — a real `.wasm` binary decoder/encoder (LEB128, all MVP sections), a stack-machine interpreter executing the full WASM numeric/control-flow instruction set (including `call_indirect` through a real table + element segments, and traps for div-by-zero/overflow/OOB/stack-exhaustion), a WAT-inspired `.kwat` text assembler with named locals/globals/labels/forward references, and "Pebble," a small imperative language (functions, if/while, recursion, short-circuit `&&`/`||`, proper lexical block scoping) with its own lexer→parser→compiler emitting real WASM binaries Kiln's own interpreter runs — plus an interactive HTML execution-trace visualizer and real WASI-lite host imports (`env.print`, a genuine `wasi_snapshot_preview1.fd_write`).
- **Stack:** Pure Python 3 stdlib only for the whole toolchain (no deps); Node.js's native `WebAssembly` engine used exclusively as an independent differential-testing oracle (`kiln verify`, `test_node_diff.py`) — every module Kiln produces can be run byte-identical on a real production WASM runtime and diffed, not just checked against itself; a self-contained single-file HTML/CSS/JS execution visualizer; Playwright/Chromium (pre-installed) for a headless UI smoke test.
- **Features shipped:** binary decoder/encoder round-tripping real `.wasm` modules byte-for-byte (type/import/function/table/memory/global/export/start/element/code/data sections); full interpreter (i32/i64/f32/f64 numerics, structured control flow, locals/globals, linear memory with correct width/sign/alignment semantics, function tables) cross-checked instruction-by-instruction against Node; `.kwat` assembler (two-pass symbol resolution, forward references, named types for `call_indirect`); Pebble compiler (recursion, mutual recursion, while-loops, short-circuit boolean ops, arithmetic precedence) running end-to-end inside Kiln's own VM; **both stretch features shipped**: an interactive HTML trace visualizer (`kiln trace --html`, play/pause/step/scrub, screenshot-verified in headless Chromium in light and dark themes) and WASI-lite host imports demonstrated by a real `fd_write`-based "hello world" (`examples/hello_wasi.kwat`); `kiln verify`/`kiln disasm` CLI commands; `demo.sh` driving all of the above through the real CLI.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch; adversarial review (Phase 3) found and fixed 6 real bugs — Pebble's variable scoping was fully flat, so two `let x` in disjoint `if`/`else` branches falsely collided (fixed with a proper scope stack); deep recursion crashed with a raw Python `RecursionError` instead of a clean WASM-style trap; the assembler never validated global mutability, silently accepting writes to `global.set` on immutable globals; every CLI subcommand leaked raw Python tracebacks for ordinary user errors (missing files, bad syntax, undefined exports); the assembler raised bare `IndexError`/`KeyError` instead of diagnosable errors for common authoring typos — all fixed with regression tests. The differential-testing strategy itself caught a bonus bug during Phase 4: the WASI-lite demo's iovec declared one byte too many, so both Kiln and Node identically printed the message plus a stray NUL byte read past its end — a bug a return-value-only check would have missed entirely, only visible by comparing actual output bytes between two independent engines. 59/59 tests green plus a 7-check `demo.sh` (unit suite, assemble→run→disasm, Pebble compile→run, Node differential verification, HTML visualizer, clean CLI error handling, byte-for-byte round-trip fidelity) all pass.

## 2026-07-14 — Loom
- **What:** A GPT-style decoder-only transformer language model built entirely from scratch in NumPy — byte-level BPE tokenizer, embeddings, causal multi-head self-attention, GELU feed-forward, weight-tied output head — with hand-written backpropagation for every op (no PyTorch/TensorFlow/JAX, no autodiff library), a real Adam training loop, temperature/top-k/top-p sampling, and a self-contained interactive HTML visualizer.
- **Stack:** Python 3 + NumPy only; self-contained HTML/CSS/JS (no server, no external libs) for the attention-heatmap + loss-curve visualizer; Playwright/Chromium used only in dev to screenshot-verify the visualizer's light/dark theming.
- **Features shipped:** byte-level BPE tokenizer trained from scratch (lossless round-trip incl. unseen text and random-byte fuzzing); GPT-style transformer (token+positional embeddings, N-layer pre-norm causal multi-head attention + GELU FFN with residuals, final layer norm, weight-tied head) in raw NumPy; hand-written backprop for every op (embedding scatter-add, linear, layer norm, the softmax-attention Jacobian, GELU's exact tanh-approx derivative, fused softmax-cross-entropy) proven correct via finite-difference gradient checking across depths/batch sizes/edge cases (seq_len=1, batch=1); Adam + LR warmup/cosine-decay + grad-norm clipping + checkpointing that demonstrably drives loss down on a bundled Shakespeare corpus; **both stretch features shipped**: temperature/top-k/top-p nucleus sampling CLI, and a light/dark-theme-aware HTML visualizer (sequential-blue per-layer/per-head attention heatmaps with a legend + hover tooltips showing exact weights and token labels, plus a training-loss line chart with a hover crosshair and direct end-label); full CLI (`tokenizer-train/gradcheck/train/sample/viz/demo`); 55-test suite + 23-check `demo.sh` driving the real CLI end-to-end.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch; adversarial review (Phase 3) found and fixed 6 real issues — the visualizer crashed instead of truncating when a prompt/model's context exceeded its hardcoded window, embedded JSON in the generated HTML wasn't escaped against a `</script>`-shaped payload (the same class of bug Palimpsest shipped and fixed on 2026-07-04), every CLI command leaked raw Python tracebacks on ordinary bad input instead of a clean error, `viz`/checkpoint writes didn't create missing parent directories, `cmd_demo` used fake ad-hoc classes instead of `argparse.Namespace`, and the visualizer had zero test coverage — all fixed with regression tests; Phase 4 polish then caught and fixed 3 more edge cases (`steps=0`, `batch_size=0`, `seq_len=0` surfaced as confusing low-level NumPy errors) and, while actually screenshotting the redesigned visualizer in light/dark mode, a real dark-mode theming bug where the page background and tooltip box sat outside the themed root element (white-on-white invisible title in dark mode); 55/55 tests green, 23/23 `demo.sh` checks green, gradcheck passes with worst relative error ~1e-4.

## 2026-07-15 — Glean
- **What:** A full-text search engine built from scratch in pure Python — a hand-written Porter (1980) stemmer verified against the algorithm's own paper, a positional inverted index with a real on-disk binary format (per-document token streams as the source of truth; `load()` rebuilds postings/lengths, not a pickle dump), a boolean/phrase/proximity query language compiled to postings-list set algebra, Okapi BM25 ranking with highlighted snippets, Levenshtein/BK-tree fuzzy "did you mean" suggestions, prefix-trie autocomplete, and a server-backed interactive browser search UI.
- **Stack:** Pure Python 3 stdlib only (no deps) for the entire engine, CLI, and JSON API server (`http.server`); a single static HTML/CSS/JS page for the UI with no client-side ranking/parsing logic; Playwright/Chromium (pre-installed, driven via Node) for a headless-browser UI smoke test.
- **Features shipped:** Unicode tokenizer + from-scratch Porter stemmer (steps 1a–5b, exact consonant/vowel measure, verified against Porter's own paper's worked examples); positional inverted index (`term -> {doc_id: [positions]}`) with incremental `add_document`, document-length tracking for BM25, and a custom binary persistence format with a build→load→re-query round-trip test; query language (bare words defaulting to OR, `AND`/`OR`/`NOT`, `"exact phrase"`, parenthesized grouping, `word1 NEAR/k word2` proximity) verified by a 300-query randomized fuzz test plus a curated battery against a brute-force oracle that re-scans each document's raw token stream; Okapi BM25 ranking (configurable k1/b, term-frequency saturation and document-length normalization both verified) + a TF-IDF cosine scorer for comparison + highlighted/extracted snippets; Levenshtein edit-distance DP + BK-tree fuzzy index powering "did you mean" suggestions on both the CLI and the browser UI; frequency-ranked prefix trie over real (unstemmed) corpus words for search-as-you-type autocomplete; interactive server-backed search UI (`http.server` JSON API + a single dark/light-aware HTML/CSS/JS page, live search-as-you-type, autocomplete dropdown, click-to-search "did you mean" chips); hand-written 50-article demo corpus across 5 topics; `glean` CLI (`index/demo-index/search/stats/serve`); 111-test suite + 22-check `demo.sh` (unit tests, CLI query-language walkthrough, persistence round trip, live JSON API, headless-Chromium UI smoke test).
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review (Phase 3) found and fixed 7 real bugs — two CRITICAL correctness bugs a user would hit constantly (document titles were never actually indexed/searchable, and `term NOT term2` silently parsed as `term OR (NOT term2)` — matching almost every document instead of excluding one), a query-lexer bug where `\S+` swallowed an adjacent `)` so parenthesized queries like `cooking AND (pasta OR bread)` failed to parse and unbalanced parens like `cat AND dog)` went undetected, a correctness gap where hyphenated/apostrophe'd query words (`well-known`, `don't`) could never match because the query parser didn't route them through the same tokenizer used at index time, a silent-failure bug where an unterminated `"phrase` quietly became an unmatchable garbage term instead of raising a clear error, a `RecursionError` crash on a long run of the letter 'y' in the stemmer (fixed by replacing self-recursion with an iterative scan), and a `--top-k -1` CLI/library argument silently hitting Python's "all but the last result" slice trap instead of erroring — all fixed with regression tests, plus one more UX bug caught only by headless-browser testing in Phase 4 (the autocomplete dropdown opened then immediately closed itself on every keystroke because the debounced search handler unconditionally closed it, a race that always lost on localhost's low latency) — fixed and re-verified. 111/111 Python tests green (incl. a 300-query differential fuzz oracle and Levenshtein symmetry/triangle-inequality property tests), 22/22 `demo.sh` checks green including a 13-check headless-Chromium walkthrough of the real UI (search, highlighting, autocomplete, tab-complete, clicking a "did you mean" suggestion, malformed-query error display).

## 2026-07-16 — Loom
- **What:** A tiny GPT-style language model built entirely from scratch — a hand-rolled reverse-mode tensor autodiff engine (not a scalar one; batched NumPy-array ops with hand-derived backward passes for broadcasting arithmetic, batched matmul, reshape/permute, softmax, layer norm, GELU, embedding gather, and cross-entropy), a byte-level BPE tokenizer trained from scratch, a decoder-only multi-head causal-attention Transformer built out of that engine, an Adam + LR-warmup/cosine-decay + gradient-clipping training loop, autoregressive sampling (greedy/temperature/top-k/top-p), and an interactive attention-weights HTML visualizer. No PyTorch, no JAX, no autograd library anywhere.
- **Stack:** Python 3 + NumPy (array math only — every autodiff op's forward *and* backward is hand-written); self-contained HTML/CSS/JS for the attention visualizer (no build step, theme-aware); Playwright/Chromium (pre-installed, driven via Node) for a headless UI smoke test; an original ~8.7 KB fable (`data/corpus.txt`) authored for this build as training data, not scraped or copyrighted text.
- **Features shipped:** from-scratch `Tensor` computation-graph autodiff engine, 17/17 ops gradient-checked against numerical finite differences incl. a full transformer block; byte-level BPE tokenizer (merge-pair training, exact encode/decode round-trip incl. unicode/emoji/empty-string, JSON persistence); GPT transformer (token+positional embeddings, N causal-MHSA+MLP blocks with residuals and LayerNorm, verified **zero causal leakage** via an explicit before/after logit diff) with per-seed-reproducible initialization; Adam optimizer w/ bias correction, warmup+cosine LR schedule, global grad-norm clipping, measurable loss decrease, `.npz` checkpoint save/reload; greedy/temperature/top-k/top-p sampling + a `chat` REPL; **stretch feature**: `attn-viz` command renders real captured per-layer/per-head attention matrices as an interactive, theme-aware, self-contained HTML heatmap (layer/head picker, hover tooltip with exact weight + token pair, causal-mask cells visually distinguished), headless-Chromium-verified in both light and dark; full CLI (`train-tokenizer/train/generate/chat/attn-viz/gradcheck/demo`); 32-test unit suite + `demo.sh` (17 end-to-end CLI checks).
- **Verdict:** Shipped. 4/4 required + 1/2+ stretch (attention visualizer; a second stretch — a loss-curve/sample-quality HTML report — was deliberately dropped in favor of fully polishing and verifying the one shipped, per REVIEW.md); adversarial review (Phase 3) found and fixed 6 real issues — worst was `--seed` silently **not** controlling weight initialization (a module-level RNG meant two different seeds produced byte-identical starting weights, only batch sampling differed — fixed by threading an explicit `np.random.Generator` from `GPT(seed=...)` through every layer), an off-by-one in corpus-length validation that crashed with a raw NumPy `ValueError: high <= 0` on a corpus exactly one token short instead of a clean message, an unhandled `IndexError` crash on `--steps 0`, raw Python tracebacks on missing/corrupt corpus/tokenizer/checkpoint files, two gradient-checked-but-dead autodiff ops (`cat`, `__getitem__`) never actually used by the model (removed), and a `Tensor.shape` plain attribute that could silently go stale (converted to a property) — all fixed with regression tests; 17/17 gradient checks and 32/32 unit tests green; `demo.sh`'s 17 checks (training, generation, chat, attention viz, and edge cases: missing files, `--steps 0`, whitespace-only prompts) all pass; honestly documented that at this corpus/model scale the pipeline demonstrably works end-to-end but the tiny model mostly memorizes its ~8.7 KB corpus rather than generalizing — disclosed in README rather than oversold.

## 2026-07-17 — Sprout
- **What:** A tiny GPT-style language model built entirely from scratch — byte-level BPE tokenizer, a full decoder-only transformer (causal multi-head self-attention, LayerNorm, GELU MLP, residual blocks) with a hand-derived **forward AND backward pass** for every op, an Adam optimizer, and temperature/top-k/top-p sampling. No PyTorch/TensorFlow/JAX and no autograd library anywhere — every gradient is verified against numerical finite-difference checks (`gradcheck.py`, 28+ checks, all passing) before a single real training step is trusted. This is the one artifact type missing from the repo's history: a generative model, not a solver or a renderer.
- **Stack:** Pure NumPy for the model/optimizer/tokenizer (no ML framework); an original ~170KB hand-templated training corpus (fables/dialogues/field notes, no scraped or downloaded text); a zero-dependency `http.server` playground (browser holds zero model logic, same server-holds-the-logic pattern as Gambit/Formulate); a self-contained interactive HTML attention-heatmap visualizer built from real per-layer/per-head weights captured off an actual forward pass.
- **Features shipped:** BPE tokenizer (train/encode/decode/save-load, byte-level so any input round-trips including unseen unicode); full transformer forward+backward (`Linear`/`LayerNorm`/`Embedding`/`CausalSelfAttention`/`MLP`/`Block`/`GPT`, einsum-based attention grad derived and gradient-checked both directions); training loop (minibatching, train/val tracking, checkpointing, live sampling during training); autoregressive generation (temperature/top-k/top-p + CLI chat mode); **both stretch features shipped**: an interactive attention-heatmap HTML visualizer (layer/head picker, query-token slider) and a web playground + auto-generated `MODEL_CARD.md` (real loss curve, perplexity, honest capability assessment, not marketing copy). Shipped checkpoint: 932,608 params, 4 layers/4 heads/128 dim, trained 1,600 steps (~43 min CPU) to val_loss 0.292 (perplexity 1.34); untrained-model sanity check (initial loss 6.256 ≈ ln(512)=6.238) independently corroborates the loss/softmax math.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch. Adversarial review (Phase 3) found and fixed 5 issues: `train.py --steps 0` silently producing a broken checkpoint with empty loss history (now validated up front), three of four CLI entry points (`server.py`/`attention_viz.py`/`model_card.py`) crashing with a raw traceback instead of a friendly message on a missing checkpoint (now consistent with `generate.py`), a real concurrency bug where `server.py`'s `ThreadingHTTPServer` shared one mutable `numpy.random.Generator` across request threads with no locking (fixed with a fresh per-request Generator), an O(vocab) Python loop in the top-p sampling filter rewritten as a vectorized NumPy mask, and a corpus generator that could pick the same animal/character name on both sides of a sentence. 22/22 unit tests green, all gradient checks green, `demo.sh` (regenerate corpus → gradcheck → tests → generate from 4 prompts → attention viz + model card → concurrency/edge-case checks) runs clean end-to-end against the shipped checkpoint. Honest limitation, stated plainly in the README/model card: at ~1M params and 170KB of highly repetitive template data, the model fluently recombines the corpus's own sentence templates rather than demonstrating general language competence — expected and disclosed, not hidden.

## 2026-07-18 — Trove
- **What:** A full-text search engine built entirely from scratch — a from-scratch Porter stemmer, an inverted index with positional postings, Okapi BM25 ranking, a hand-written recursive-descent boolean/phrase/prefix query parser, a Levenshtein/BK-tree fuzzy typo corrector, a trie-based autocomplete engine, densest-cluster relevance-snippet extraction, and a Precision@K/NDCG@K ranking-quality evaluator, behind a CLI and a server-backed interactive web UI (same zero-client-logic architecture as Formulate/Gambit). Its demo corpus is this repository's own history — the 36 past projects' READMEs and every LEDGER.md entry, made properly searchable for the first time instead of `grep`-only. A classical, non-neural complement to VecNN's (2026-06-27) vector/embedding search, not a repeat of it.
- **Stack:** Pure Python 3 stdlib only for every runtime code path (`re`, `json`, `http.server`, `math`, `argparse`) — zero pip dependencies to run or ship; NLTK used exclusively as a dev-time differential oracle in the test suite to independently verify the from-scratch Porter stemmer (never imported by shipped code), the same pattern as past builds cross-checking against real `git` or OpenSSL; Playwright/Chromium (pre-installed) for a manual headless-browser UI check.
- **Features shipped:** tokenizer + inverted index with a Porter stemmer differentially verified against NLTK's `ORIGINAL_ALGORITHM` oracle across ~5,100 unique real words (100% match) and automatic markdown-header document-splitting (one `LEDGER.md` becomes 40+ independently-rankable documents); Okapi BM25 ranking hand-verified against an independently-written textbook formula on a fixture index; a real query language (`AND`/`OR`/`NOT`, parentheses, implicit `AND`, `"exact phrase"` via positional adjacency, `term*` prefix); fuzzy typo correction via a from-scratch BK-tree with a length-scaled auto threshold (no correction under 4 characters to avoid short-word false positives); **all 3 stretch features shipped**: a frequency-ranked autocomplete trie (also accelerating prefix queries), relevance snippets that find the densest cluster of query-term hits rather than just truncating, and a ranking-quality evaluator measuring **mean P@5 = 0.86, mean NDCG@5 = 0.96** over 14 hand-labeled real queries (enforced as a regression floor, imperfections left visible rather than curated away — e.g. two unrelated same-named "Strata" projects legitimately rank near each other); a dependency-free `http.server` web UI with live search/autocomplete/snippets, verified XSS-safe both structurally (the tokenizer strips `<`/`>` before indexing, so a literal `<script>` tag can't survive into a snippet) and with a live payload document in `tests/test_server.py`; `trove build/search/serve/eval` CLI; `demo.sh` running the full verification suite.
- **Verdict:** Shipped. 4/4 required + 3/3 stretch; adversarial review (Phase 3) found and fixed 8 real bugs — worst was a **silent-wrong-answer bug**: `-(fox AND lazy)` returned the same result as the positive `(fox AND lazy)` instead of its complement, because the lexer couldn't tokenize `-` fused directly to `(` or `"` (also affected `-"phrase"`) — reproduced, fixed, and given a direct regression check in both the unit suite and `demo.sh`. Other fixes: a duplicate-`doc_id` crash risk that's realistic for this specific repo (which already has several same-named projects from parallel sessions), an unterminated-quote query silently reinterpreting instead of erroring, unbounded recursion depth on pathological nested-parenthesis input, Python's negative-slice semantics silently mangling `--top -1`, fixed-distance fuzzy correction producing nonsense on short words, a case-sensitive `--ext` flag silently matching zero files, and a stemmer function re-sorting a constant list on every call. 126/126 tests green; `demo.sh`'s 11 sections (unit suite, BM25 ranking, boolean/OR queries, phrase adjacency, prefix queries, fuzzy correction, negation incl. the `-(...)` regression check, the ranking evaluator, all 3 live web-UI API endpoints, and a second independent non-software corpus) all pass end-to-end against the real repo-history index.

## 2026-07-19 — Loom
- **What:** A transformer language model built entirely from scratch — a reverse-mode **tensor** autodiff engine (numpy-backed, broadcasting-aware, no PyTorch/TensorFlow/JAX/autograd of any kind), a GPT-style decoder-only transformer built on top of it (causal multi-head self-attention, pre-norm LayerNorm, residual streams, GELU feedforward, dropout), a real Adam training loop with LR warmup/cosine decay and best-validation-loss checkpointing, and autoregressive generation with temperature/top-k/top-p sampling. The first build in this ledger to tackle the Transformer, rather than staying at the "classical algorithm" layer like Cotangent's scalar-autodiff MLP or QSim's quantum circuits.
- **Stack:** Python 3 + numpy (array math only — every op's forward *and* backward pass is hand-written, no autodiff library of any kind); self-contained HTML/Canvas/JS for the attention visualizer; Node/Playwright (pre-installed) for a headless-browser smoke test of the generated viz JS only.
- **Features shipped:** from-scratch `Tensor` autodiff engine (add/sub/mul/div/matmul/reshape/permute/gather/slice/concat/sum/mean/exp/log/tanh/relu/gelu/softmax/layernorm, all broadcasting-aware) — every op independently gradient-checked against numerical finite differences (26/26), plus an end-to-end check perturbing a real weight inside a real forward pass through the whole model; GPT transformer (token+positional embeddings, N causal multi-head-attention blocks, residual+pre-norm, GELU feedforward, final vocab projection) built entirely on that engine; Adam (bias-corrected) + warmup/cosine LR schedule + gradient clipping + dropout training pipeline that ships the best-validation-loss checkpoint rather than just the final (likely overfit) step's; temperature/top-k/top-p autoregressive sampling CLI; **both stretch features shipped**: an interactive HTML attention-heatmap visualizer that replays a real generation run's actual per-layer/per-head softmax(QK^T/√d) output (verified in headless Chromium, light+dark, zero console errors), and a from-scratch byte-level BPE tokenizer (merge-pair training loop, correctly learns the most-frequent pair first, lossless round-trip on in-corpus/out-of-corpus/non-ASCII/empty strings); 53-test suite (autodiff, model, optimizer, tokenizers, checkpoint round-trip, sampling, CLI error handling, browser viz) + `demo.sh`; trained checkpoint + a real generated attention-viz HTML committed and shipped.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch; adversarial review (Phase 3) found and fixed 9 real bugs — worst was `Tensor.backward()` crashing on any op mixing a non-differentiable leaf into a differentiable one (e.g. division by a constant), because the topological sort called every node's local backward closure unconditionally even when its `.grad` was never populated (fixed: skip nodes with `requires_grad=False`); also a BPE/attention-viz crash (`generate()` assumed a `CharTokenizer`-only attribute), an unsafe empty-prompt seed that would emit a raw null byte under the BPE tokenizer, a bare `KeyError` on out-of-vocabulary prompt characters, a script-injection gap in the HTML visualizer (unescaped `</script>` in generated text could break out of its embedding `<script>` block — the same bug class Palimpsest's review flagged as its worst finding), a default-checkpoint-path mismatch that would have broken the documented zero-args quickstart, and only-the-final-checkpoint-ever-shipped (fixed with best-val-loss tracking, confirmed necessary against the real run: best val_loss 1.7915 at step 600/4000, rising to ~2.8 by step 4000 even with dropout) — all fixed with regression tests; 53/53 tests green, all 26 autodiff ops plus the full end-to-end model gradient-checked, and the shipped checkpoint generates real, correctly-spelled, thematically-on-corpus English word fragments (not yet grammatical sentences — an honestly-disclosed ceiling for a ~207K-parameter char-level model on 17.7 KB of text, not a bug).

## 2026-07-20 — Impulse
- **What:** A 2D rigid-body physics engine built entirely from scratch — real semi-implicit Euler integration, a spatial-hash broad phase, SAT narrow-phase collision detection with clipped contact manifolds (circle-circle, circle-polygon, polygon-polygon, up to 2 contact points per polygon pair), a sequential-impulse solver (restitution + Coulomb friction + Baumgarte positional correction), and three joint types (rigid distance joints, damped springs, point-to-point pin joints) — driven live by a stdlib HTTP server and rendered in a real-time interactive browser sandbox (drag-to-spawn, grab-and-fling, scene presets with full joint-aware save/load, a live debug overlay showing the actual broad-phase grid).
- **Stack:** Pure Python 3 stdlib only (no numpy/pymunk/pygame) for the entire engine and server, including a hand-rolled Server-Sent-Events stream over `http.server`; a single static HTML/CSS/vanilla-JS canvas page for the UI with zero client-side physics — the browser only renders whatever the server computes; Playwright/Chromium (pre-installed) for interactive UI verification (drag-spawn, grab-and-fling, debug overlay, scene round-trips).
- **Features shipped:** rigid body dynamics for circles and convex polygons with correct per-shape mass/inertia; spatial-hash broad phase + SAT narrow phase with clipped contact-manifold generation; sequential-impulse solver with restitution, Coulomb friction, and Baumgarte correction (10 iterations/step); DistanceJoint/SpringJoint/PinJoint; a real-time sandbox (drag-to-spawn sized by drag distance, a grab tool with a live mouse-spring that correctly picks the topmost body in an overlapping stack, material/world sliders, pause/step/reset, live FPS/body-count HUD); **both stretch features shipped**: four preset scenes (pyramid, rope bridge, circle stack, Newton's cradle) plus full JSON scene export/import that round-trips joints too (a saved bridge loads back as a bridge, not disconnected planks), and a debug overlay showing contact points, contact normals, per-body velocity vectors, and the actual broad-phase grid the engine uses.
- **Verdict:** Shipped. 5/5 core features (treated the plan's 4-required minimum as 5, since together they're the floor for a physics engine that's actually a physics engine) + 2/2 stretch. Adversarial review found and fixed 8 issues, the worst a critical one: a single non-finite (NaN) coordinate — reachable from a hand-edited scene import, a fast mouse-drag frame, or a malformed `/api/grab` call — reached an unguarded `int(math.floor(x/cell))` in the broad phase and permanently killed the unsupervised physics thread, silently freezing the entire sandbox for every connected client with zero user-facing error; reproduced live three separate ways before being fixed with input validation at every network boundary plus a supervisory try/except around the step loop as defense in depth. Also found and fixed: scene export silently dropped every joint (loading a saved bridge produced a pile of disconnected planks — the primary point of a joint-aware save/load feature, defeated), an unbounded Baumgarte velocity-bias term that launched overlapping spawns off-scene at extreme velocity, and a grab-tool z-order bug that picked the visually-bottom body in an overlapping stack instead of the top one. 30/30 unit tests green (vector math, per-shape mass/inertia, every collision-pair type, restitution across e=0/0.5/0.95, friction on/off, all three joint types including an energy-boundedness check on a 30-second pendulum simulation), a 10-check headless demo (box stack settling, pin-joint pendulum, rope bridge sagging and holding a dropped boulder), and a live HTTP server smoke test (spawn-and-fall over real network calls, all four scenes load, NaN-import regression guard) all pass via `demo.sh`.

## 2026-07-21 — Waveforge
- **What:** A software synthesizer + step sequencer built from raw sample-array math — no audio codec library, no Web Audio synthesis nodes. Phase-accumulator oscillators (sine/saw/square/triangle/noise), an ADSR envelope, a real biquad low-pass filter (RBJ cookbook design), a feedback delay + Schroeder reverb (4 parallel combs + 2 series allpasses), and a hand-written 44-byte RIFF/WAVE header + 16-bit PCM encoder. The entire DSP core (`dsp.js`/`sequencer.js`) has zero browser dependency, so it runs unmodified in Node (CLI, tests) and in a plain `<script>` tag (browser UI) — no bundler, no framework.
- **Stack:** Vanilla JS (no deps, no build step) for the engine, CLI, and browser UI; Node's `fs`/`child_process` for the CLI/tests; Playwright/Chromium (pre-installed) for a headless-browser UI smoke test.
- **Features shipped:** multi-waveform phase-accumulator oscillator engine; ADSR envelope + resonant biquad low-pass filter per voice (plus an optional exponential pitch-drop envelope, used for the kick's characteristic thump); multi-track polyphonic step sequencer (arbitrary track count mixed additively, swing timing, 7 built-in instrument patches — kick/snare/hat/bass/lead/pad/pluck); from-scratch WAV export (CLI writes to disk, browser downloads a Blob) with a round-trip header test; **both stretch features shipped**: a delay+reverb effects chain (verified to add a real, measurably audible decay tail past where the dry signal goes silent) and patch save/load as JSON with a "Custom (loaded)" indicator so the UI's preset dropdown never misrepresents the active patch; an interactive dark-synth-styled browser step sequencer (16-step grid, 4-level velocity click-cycle, per-track waveform/ADSR/filter/gain controls, play/stop/export transport, demo-song loader).
- **Verdict:** Shipped. 4/4 required + 2/2 stretch; adversarial review (Phase 3) found and fixed 4 real bugs — two crashes reachable from hand-authored pattern files (`bpm <= 0` blew up `Float32Array` allocation with a cryptic `Invalid typed array length: Infinity`; a patch missing `envelope` crashed deep in the DSP core instead of failing at the boundary with a message naming the track — both now validated up front with clear errors) plus two UI-fidelity bugs (demo-loaded step velocities didn't match the interactive grid's 4-level click-cycle, so clicking a step jumped straight to "off" instead of advancing; per-step `hold`, used by the pad track's 8-step sustain, was silently dropped when the demo song was loaded into the browser, making it play differently than the CLI render of the same file) — both fixed with velocity-snapping and a richer per-step data shape. Writing the Phase 5 test suite caught two more: the triangle oscillator was phase-shifted from the sine/saw/square convention (started at -1, peaked at phase 0.5, instead of starting at 0 and peaking at 0.25) — fixed so switching waveforms never introduces a phase discontinuity at note-on. 33/33 unit tests green (oscillator shapes, envelope math, filter rolloff, WAV header bytes, polyphonic mixing energy, effects tail, every Phase 3 regression, CLI exit codes) plus a headless-Chromium UI smoke test (add/remove track, step click-cycle, play/stop, demo load, WAV export) — all pass via `./demo.sh`.

## 2026-07-22 — Loom
- **What:** A tiny GPT-style decoder-only transformer language model built entirely from scratch — no PyTorch/TensorFlow/JAX anywhere. A hand-built reverse-mode tensor autograd engine (NumPy arrays + backward tape, every op gradient-checked against finite differences) underpins a from-scratch byte-level BPE tokenizer, causal multi-head self-attention, a from-scratch Adam optimizer, and temperature/top-k/top-p autoregressive sampling with an optional KV-cache — plus a server-backed interactive playground with live attention-heatmap visualization.
- **Stack:** Python 3 + NumPy only (no ML framework of any kind); stdlib `http.server` for the playground backend; a single static HTML/CSS/JS page for the UI with zero client-side model logic; Playwright/Chromium (pre-installed) for a headless browser smoke test of the playground.
- **Features shipped:** `Tensor` autograd engine (matmul w/ batched broadcasting, broadcasting add/sub/mul/div, reshape/transpose, softmax, layer norm, GELU, embedding gather, cross-entropy — all gradient-checked, not just "it ran"); byte-level BPE tokenizer trained from scratch (lossless round-trip verified on seen text, unseen text, emoji, empty string, pathological repeats); transformer (causal multi-head attention, pre-LN residual blocks, weight-tied output head) + training loop (Adam from scratch, warmup+cosine LR schedule) trained on an original 2,300-word corpus written for this project (loss 6.09→3.15 over 2000 steps, ~6.7 min CPU, samples visibly improve from random bytes to real word fragments); temperature/top-k/top-p sampling; **both stretch features shipped**: KV-cache incremental decoding (verified bit-identical to full recompute under greedy decoding, ~2.7x measured speedup) and an interactive server-backed playground (live per-layer/per-head attention heatmaps, training-loss curve, before/after-training sample comparison, browser-tested end-to-end with Playwright); `train.py/sample.py/server.py/benchmark.py` CLI suite; 24-test suite (autograd/tokenizer/model) + `demo.sh` (8-step full pipeline verification: tests, a fresh from-scratch training run, KV-cache parity, the benchmark, and a live server API walkthrough).
- **Verdict:** Shipped. 4/4 required + 2/2 stretch; corpus is self-authored rather than fetched from Project Gutenberg because this environment's egress policy blocks that domain (confirmed via a genuine 403 policy denial, not retried per policy) — sidesteps licensing ambiguity entirely; adversarial review (Phase 3) found and fixed 2 real bugs — CRITICAL: KV-cache prompt-priming (feeding a multi-token prompt into the cache in one call) skipped the causal mask entirely because the masking branch checked "is a cache present" instead of "is there more than one new query token," letting later prompt tokens attend to earlier ones' future — fixed by rebuilding the mask from absolute positions uniformly for every call, cache or not; and a generation-path divergence where a prompt at/over the context window made the cached path emit zero new tokens and the non-cached path emit exactly one before an early-exit check fired on the wrong (unbounded) length — fixed by truncating the prompt to the context window exactly once up front so both paths are provably identical for any input; both caught by consistency tests (cached vs. non-cached must produce byte-identical output under greedy decoding) rather than crashes, and REVIEW.md also documents two of the review's own test-design pitfalls (a softmax gradient check with a provably-zero-everywhere objective; a weight-tying check whose uniform perturbation was invisible against an untrained model's exactly-zero-mean LayerNorm output) that were themselves caught and fixed before they could hide a real bug. 24/24 unit tests green, all 8 demo.sh steps green, KV-cache measured at ~2.7x faster than full recompute, playground browser-tested with Playwright (screenshots confirm a genuinely designed dark-theme UI, not default-styled).

## 2026-07-23 — Vignette
- **What:** A from-scratch baseline JPEG-style lossy image codec in pure Python — the transform-coding pipeline (block DCT, quantization, Huffman entropy coding) behind real-world image/video compression, implemented down to genuine, spec-valid `.jpg` file bytes: RGB → YCbCr → 4:2:0 chroma subsampling → 8×8 block DCT → quantization → zigzag scan → DC DPCM + AC run-length → Huffman coding → real JFIF marker-framed bytes, and the full inverse pipeline back to pixels.
- **Stack:** Pure Python 3 stdlib only (no PIL, no numpy, no deps) — hand-rolled DCT, Huffman coder, and PNG encoder; Playwright/Chromium (pre-installed) used only to screenshot-verify the two HTML visualizers during development, not a runtime dependency.
- **Features shipped:** real baseline JFIF encoder producing marker-framed bytes any viewer opens (independently confirmed as a genuine JPEG by the system `file` utility) with correct MCU padding for arbitrary non-multiple-of-16 image sizes; a general baseline decoder (parses real SOI/APP0/DQT/SOF0/DHT/SOS/EOI marker segments, handles any 1×/2× H/V sampling factors) that round-trips its own encoder's output and rejects malformed input with clear errors instead of crashing or hanging; a quality/rate-distortion toolkit (PSNR, windowed SSIM, compression ratio, bpp) swept across quality settings via `vignette analyze`; an interactive self-contained HTML report with a live quality slider (size/PSNR/SSIM readouts), a 6×-zoom blocking-artifact panel, and a DCT-coefficient magnitude heatmap; **both stretch features shipped**: real per-image-optimal canonical Huffman tables (spec-legal custom DHT segments, verified bit-identical reconstruction vs. the standard tables while being ~19–25% smaller) and a quality-ladder artifact gallery (100→1) visually demonstrating the blocking→ringing→chroma-bleed progression; five deterministic procedural test images (no external image files needed) plus binary PPM I/O.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch. Adversarial review (Phase 3) found and fixed a critical quantization-table natural-order/zigzag-order indexing bug that was completely invisible at quality 100 (every quant divisor coincidentally scales to 1 there) but silently corrupted output at every other quality — caught only by testing a quality *sweep* instead of a single happy-path setting, and pinned down with a dedicated regression test afterward; also fixed an HTML report CSS layout bug (a long caption pushed a sibling image's container wider than the image, letting the page background bleed through) and a CLI UX gap (raw tracebacks on missing/malformed files, now clean `error:` messages with exit 1). 50/50 tests green (DCT vs. brute-force reference, Huffman prefix-freedom + byte-stuffing, PNG structural correctness incl. a from-scratch unfilter proving pixel recovery, full codec round-trips across every test image/quality/edge case) plus `demo.sh` exercising every CLI feature end-to-end including the external `file`-utility cross-check.

## 2026-07-24 — Loom
- **What:** A GPT-style language model built entirely from scratch — a hand-derived reverse-mode automatic differentiation engine (a tensor-valued micrograd), a from-scratch byte-pair-encoding tokenizer, a decoder-only transformer (causal multi-head self-attention, pre-LN blocks, GELU MLP, weight-tied LM head) built only from that autodiff engine's primitives, and a from-scratch Adam optimizer — trained end-to-end on the public-domain Tiny Shakespeare corpus, with a live web UI for generation and a real per-layer/per-head attention-weight visualizer.
- **Stack:** Pure Python 3 + NumPy (used only as a fast ndarray/BLAS matmul substrate — no PyTorch/TensorFlow/JAX/tinygrad/scikit-learn/transformers anywhere; every gradient in `loom/engine.py` is hand-derived); stdlib-only `http.server` backend (no Flask/FastAPI); self-contained vanilla HTML/CSS/JS frontend (no CDN, no build step); Playwright/Chromium (pre-installed) for a headless-browser UI smoke test.
- **Features shipped:** reverse-mode autodiff `Tensor` (add/mul/matmul with broadcasting, reshape/transpose, sum/mean, exp/log, relu/gelu, softmax, layer_norm, embedding lookup with scatter-add backward, dropout, cross-entropy) — every op gradient-checked against finite differences, plus a full-model check against sampled numerical gradients matching to 1e-12; from-scratch BPE tokenizer (word-frequency-weighted merge training for speed on a megabyte-scale corpus, byte fallback so encoding never fails on unseen/unicode/binary input, deterministic training, JSON save/load); GPT-style transformer (186,944 params: 3 layers, 4 heads, 64-dim embeddings, 64-token context, 512-token vocab) trained 3000 Adam steps on Tiny Shakespeare, loss dropping from the random-init baseline `ln(512)=6.24` to train 3.47 / val 3.65; autoregressive generation with temperature/top-k/top-p sampling implemented directly on the raw logit array; **both planned stretch features shipped**: an interactive web UI (live-updating training-loss chart, generation panel with adjustable sampling params, all via real HTTP calls to the loaded checkpoint) and an attention-weight heatmap visualizer with click-to-switch layer/head selectors, verified with zero console errors in both light and dark themes via a headless-Chromium smoke test; unified `loom` CLI (`generate/serve/attention/train`); `demo.sh` running the full verification suite.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch; adversarial review (Phase 3) found and fixed 4 real bugs — a server crash (HTTP 500 with a leaked traceback) when `/api/generate` received non-finite (`NaN`/`Infinity`, which Python's `json` module accepts by default) or wrong-typed numeric fields, reaching all the way into `numpy.random.choice` before failing (fixed with a validating `_numeric_field()` helper mapped to a clean HTTP 400); a CLI bug where `loom train --out ... --vocab-size ...` failed with "unrecognized arguments" due to a known CPython argparse/subparsers interaction with `nargs=REMAINDER` (fixed by intercepting `train` before argparse ever parses it); `loom generate`/`loom attention` crashing with a raw `FileNotFoundError` traceback on a missing checkpoint instead of the clean error `loom serve` already gave (fixed with a shared helper); and weight decay incorrectly applied to 1D LayerNorm gain/shift params (fixed to matrices only, with the reasoning for not discarding the already-90%-complete real training run documented in REVIEW.md). Also independently hit and fixed the exact `set -e`/`pipefail`/`| head` SIGPIPE bug a previous ledger entry (Strata, 2026-06-28) had flagged as a recurring class of mistake — caught this time by actually running `demo.sh` to completion and checking its exit code, not just skimming the output. 17/17 engine gradient-check groups, 6/6 tokenizer tests, 10/10 model/generation tests (incl. a causal-mask information-leak test and a tiny-batch overfit-to-<0.001 correctness gate), and 2/2 integration test groups (a real small end-to-end training run plus a real subprocess `loom.server` driven over actual HTTP) all green; `demo.sh` exits 0 end-to-end against the shipped checkpoint.

## 2026-07-25 — Loom
- **What:** A GPT-style decoder-only Transformer language model built entirely from scratch in Python + NumPy — byte-level BPE tokenizer (train/encode/decode), multi-head causal self-attention + LayerNorm + GELU MLP with every `backward()` derived by hand (no `torch`/`jax`/`transformers`/autograd anywhere), AdamW + warmup/cosine LR + gradient clipping, and temperature/top-k/top-p sampling — trained end-to-end on an original, procedurally generated corpus of fables (network access to real public-domain text sources was blocked in this sandbox, so the corpus is honestly synthetic rather than a memory-reconstructed transcription) and shipped with a KV-cached fast generation path and an interactive attention-heatmap + step-through-generation HTML visualizer.
- **Stack:** Python 3 + NumPy only (NumPy supplies matmul/elementwise ops, nothing else — no ML framework, no autograd); self-contained HTML/CSS/JS for the visualizer; Node/Playwright (pre-installed Chromium) for a headless UI smoke test.
- **Features shipped:** from-scratch BPE tokenizer (exact round trip incl. empty string and multi-byte UTF-8, deterministic training); full Transformer forward+backward verified against finite-difference numerical gradients for every layer individually and for the complete tied-embedding/output-head model (max relative error ~1e-5 across 84 sampled parameters); real training loop — loss fell 5.97 → ~0.18 over 2500 steps with train/val tracking closely; temperature/top-k/top-p autoregressive sampling; **both stretch features shipped**: KV-cache generation (6-7× faster than the naive path in the benchmark run, with byte-for-byte identical output to the uncached path checked automatically, not just visually similar) and an interactive HTML visualizer (per-layer/head attention heatmaps where the causal mask is visible as the empty upper triangle, plus a play/pause step-through of next-token probabilities), screenshot- and headless-Chromium-verified in both light and dark themes with zero console errors.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch. Adversarial review (Phase 3) found and fixed 6 real issues: a corpus-generator bug where fable titles named an animal sampled independently of the body's actual protagonist (titles routinely wrong — the first trained model faithfully reproduced this same incoherence, a strong signal the bug was real, not cosmetic; fixed and the model retrained from scratch on the corrected corpus); a KV-cache init bug where the "no cache yet" sentinel (`None`) was indistinguishable from "not using the cache at all," crashing the very first cached generation call; a silent-corruption path where NaN/Inf gradients compared `False` to everything in Python and so skipped clipping entirely, letting a diverged run corrupt every parameter with no error (now raises `FloatingPointError` naming the offending tensors); a CORS console error the visualizer threw on every load of its own documented primary use case (opening the file directly); an unhelpful raw traceback on a missing checkpoint dir; and one piece of dead code. Also measured and documented rather than hid two real model-level limitations: mid-generation protagonist drift (1/5 sampled fables at different seeds) and weak continuations for prompts that don't look like a fable opening. 4/4 test files green (gradcheck, tokenizer, model incl. causal-mask-leakage and overfit-sanity checks, generation incl. cache/naive parity) plus a 7-step `demo.sh` exercising every feature end-to-end against the real shipped checkpoint.

## 2026-07-26 — Glean
- **What:** A from-scratch full-text search engine — real tokenization, a from-scratch Porter (1980) stemmer, a persistent inverted index with delta-compressed varint postings, boolean (`AND`/`OR`/`NOT`) and exact-phrase queries with position-verified adjacency, BM25 relevance ranking, and typo-tolerant fuzzy matching via a Levenshtein/BK-tree — plus a CLI and a real web search UI. Its flagship demo indexes this repository's own multi-month build history, making every prior project's README/PLAN/REVIEW instantly searchable.
- **Stack:** Pure Python 3 stdlib only (no `whoosh`, no `sqlite` FTS5, no `nltk`); a single static HTML/CSS/JS page for the web UI served by a stdlib `http.server` backend; Playwright/Chromium (pre-installed) used only in this session to visually verify the UI in light and dark mode.
- **Features shipped:** analyzer pipeline (Unicode-normalizing tokenizer, stopword removal, from-scratch Porter stemmer validated exactly against Martin Porter's own 75-word published test vocabulary, including faithfully reproducing the algorithm's known quirks like `consensus`→`consensu`); two-stage persistent index (mtime-cached per-document forward analysis + inversion into a term dictionary and a binary postings file using delta-compressed varints — real inverted-index disk shape) with correct incremental reindexing (reuse-unchanged / reanalyze-changed / drop-deleted, all counted and reported); boolean query engine (Lucene-style MUST/SHOULD/MUST_NOT resolution, `"exact phrase"` requiring true positional adjacency not mere co-occurrence, pure-`NOT` queries correctly searching the whole corpus minus the exclusion) with BM25 ranking (k1=1.5, b=0.75, provably non-negative idf); fuzzy typo correction (Levenshtein edit distance + a BK-tree over the vocabulary, triangle-inequality pruned, "did you mean" suggestions) that deliberately never rewrites quoted phrases; CLI (`index/search/stats/serve/demo`) plus a dark/light-mode-aware single-page web UI (highlighted snippets with Markdown-noise stripped, live doc/term counts, clickable fuzzy suggestions) visually verified via headless-Chromium screenshots; self-indexing `demo` command that indexes `daily-builds/` itself (116 files) into an index kept entirely inside its own project folder and runs 6 real queries against 35+ prior projects' real documentation.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch; adversarial review (Phase 3) found and fixed 4 issues — a stemmer crash (`UnboundLocalError`) on essentially every `-ed`/`-ing` word, pure-`NOT` queries silently returning zero results instead of "everything except X", fuzzy correction silently rewriting words inside quoted phrases (violating the "exact phrase" contract), and a self-referential demo bug where the fuzzy-matching sample query happened to appear verbatim in this project's own `PLAN.md`, making the demo "prove" fuzzy matching by exact-matching its own planning doc — caught only by manually checking *why* each result ranked where it did, not just that results existed (and the first fix attempt recreated the identical bug one paragraph later in `REVIEW.md` itself, caught in the same pass). Phase 5's 81-test suite then caught a fifth, more serious bug adversarial review had missed entirely: `A AND B` only actually required `B` — the term before an explicit `AND` silently stayed an unfiltered "should" clause — invisible in the Phase 3 corpus because the AND-right term happened to be selective enough alone, exposed by a test built specifically to make the AND-left term the only thing that should exclude a match. 81/81 tests green (analyzer/index/query/fuzzy/crawler/snippet/CLI) plus `demo.sh`'s full walkthrough (CLI + HTTP API + self-index demo) all pass.

## 2026-07-27 — Unify
- **What:** A Hindley-Milner type inference engine, built entirely from scratch — the algorithm behind ML/Haskell/OCaml (and, in descendant form, Rust's and TypeScript's inference): given source with zero type annotations, Algorithm W derives the single most general type of every expression, catches type errors before anything runs, and lets a function like `id` be used at `int` and at `bool` in the same program. A genuinely new domain for this repo: every prior "language"-shaped build (Coil's bytecode VM, RegexLab's automata, nine separate from-scratch transformers) has been a runtime or a data structure, never a static analysis that runs *before* the program does.
- **Stack:** Pure Python 3 stdlib only, no dependencies of any kind — hand-written lexer/recursive-descent parser, unification with an occurs check, let-generalization, a tree-walking evaluator; stdlib `http.server` for the visualizer backend (zero client-side inference logic, same server-backed pattern as this repo's Gambit/Formulate/Trove); Playwright/Chromium (pre-installed) used only in this session to screenshot-verify the visualizer.
- **Features shipped:** lexer + recursive-descent parser for a small ML-like language (`let`/`let rec ... in`, curried `fun`, `if/then/else`, tuples, lists via `::`/`[]` sugar, `Some`/`None`, `match` pattern matching); Algorithm W inference (unification threading a composable substitution, occurs check rejecting infinite types like `fun x -> x x`, generalize/instantiate implementing real let-polymorphism, a full derivation-tree trace mirroring the typing judgment at every node); a tree-walking evaluator running well-typed programs to real values (genuine recursion, short-circuit `&&`/`||`, truncating integer division) with no type dispatch since a well-typed program can't go wrong; span-anchored diagnostics (every rejection names the two conflicting types with consistent expected/found wording and shared type-variable naming, plus a source-line caret); **both stretch features shipped**: a REPL with persistent, fully-generalized `let`-bindings across statements and a 7-function standard-library prelude (`map`/`filter`/`foldl`/`length`/`append`/`reverse`/`assoc`) written in Unify itself and type-checked at startup, and a server-backed HTML derivation-tree visualizer (type an expression, see its principal type, evaluated value, and the full Algorithm W judgment tree, computed server-side and screenshot-verified in a real headless browser).
- **Verdict:** Shipped. 4/4 required + 2/2 stretch. Adversarial review (Phase 3) found and fixed 7 real bugs, the most serious by far being one only the *planned* Phase 4 REPL would have triggered: `infer_program` created a fresh Algorithm W variable counter on every call, so a REPL calling it repeatedly against a persisted environment of generalized schemes could mint a "fresh" variable that numerically collided with a scheme's own bound-variable label — `instantiate()` then built a self-referential substitution (`{1: TVar(1)}`) and `apply()` recursed into itself forever, deterministically reproduced and fixed *before* the REPL was built on top of it, by threading a session-wide counter through `start_counter`/`next_counter`. Also fixed: type-error messages had "expected"/"found" backwards for every operator due to no fixed argument-order convention; `&&`/`||` didn't actually short-circuit (both operands were always evaluated first); comparing two functions with `=` silently returned `False` instead of erroring, because Python's dataclass-generated equality on closures never raises; paired type names in one error message could be printed with inconsistent variable letters since `pretty()` restarted its naming per call; a missing input file and stack-depth overflows both crashed with raw Python tracebacks instead of clean diagnostics. Two remaining "sharp edges" (`f -1` parsing as subtraction, not application; a non-final `match` case's body swallowing the next case) are the same well-known ambiguities OCaml/F#/SML have for the same structural reason, documented rather than "fixed" out of the language's real ML lineage. 119/119 unit tests green plus a 34-check end-to-end demo (`demo.py`) exercising every required and stretch feature against real assertions — including spinning up the actual visualizer server and hitting it over real HTTP — all passing.

## 2026-07-29 — Ember
- **What:** A from-scratch **JIT compiler**: a small C-like language compiled directly to real x86-64 machine code — correct REX/ModRM/SIB encoding, a genuine System V AMD64 calling convention (rdi/rsi/rdx/rcx/r8/r9, 16-byte call-site stack alignment tracked at compile time), real backpatched jumps/calls — assembled into an `mmap(PROT_EXEC)` buffer and invoked directly via `ctypes.CFUNCTYPE`. No bytecode layer, no interpreter loop in the hot path: once compiled, an Ember function *is* machine code, run by the real CPU. A genuinely new domain for this repo: every prior "language" build (Coil's bytecode VM, Kiln's WASM interpreter, nine from-scratch transformers, Unify's tree-walking evaluator) targeted a runtime or a data structure, never handed the CPU actual instructions.
- **Stack:** Pure Python 3 stdlib only (`ctypes`, `mmap`, no third-party deps) for the entire lexer/parser/interpreter/encoder/JIT; the box's real `gcc` and `objdump` used exclusively as independent verification oracles (an AST→C transpiler feeds `gcc -O2 -fwrapv`, and the raw emitted bytes are fed straight to `objdump -b binary -m i386:x86-64`); self-contained HTML/CSS for the report (no client-side compiler logic); Playwright/Chromium used only in this session to screenshot-verify the report.
- **Features shipped:** hand-written lexer + recursive-descent/precedence-climbing parser with positioned errors; a static semantic-check pass (`semcheck.py`) both the interpreter and JIT run up front, giving the two backends one shared, control-flow-independent notion of program validity; a tree-walking reference interpreter with 64-bit two's-complement wraparound arithmetic (matching real hardware, not Python's arbitrary precision) as the ground-truth oracle; the x86-64 encoder/codegen itself (stack-slot locals, real function calls/recursion/mutual recursion, a 6-parameter limit that forces correct r8/r9 REX-bit handling both at a callee's prologue and at a caller's argument-marshalling site, guarded division with clean `EmberRuntimeError`s for divide-by-zero and INT64_MIN/-1 instead of a raw SIGFPE); a differential harness checking interpreter/JIT/gcc agree bit-for-bit across Fibonacci, factorial (iterative+recursive), GCD, mutual recursion, primality testing, Ackermann, and short-circuit-proof programs; **all 3 planned stretch features shipped, plus a 4th**: the objdump disassembly oracle, the gcc-transpilation oracle, an interactive dark/light-aware HTML report (source/AST/real-objdump-listing per example plus a genuinely measured interpreter-vs-JIT benchmark — 1,300-1,700× on recursive Fibonacci, not an invented number), and a constant-folding optimizer (`--opt`) that folds literals and short-circuit-decided branches at compile time while *never* folding away a divide-by-known-zero so the runtime guard still fires (shrank a real example from 466 bytes of machine code to 133).
- **Verdict:** Shipped. 4/4 required + 4 stretch (2 required, 3 planned). Adversarial review (Phase 3) found and fixed 4 real bugs, the worst being the interpreter and JIT *disagreeing about whether a program was even valid*: an assignment to an undeclared variable inside an untaken `if` branch made the interpreter silently return a normal result (Python only walks the taken branch) while the JIT raised (codegen must emit code for every statement, taken or not) — fixed with a shared static semantic-check pass both backends now run first, rather than each backend lazily discovering validity problems on its own schedule. Also fixed: two duplicate function definitions crashing with a raw `AssertionError` from the assembler's own label bookkeeping instead of a clean error; calling an undefined function crashing the JIT with a raw `KeyError` from the linker's fixup-resolution step; and a 3,000-deep left-associative expression chain crashing both backends with a raw Python `RecursionError` (fixed with an explicit, documented nesting-depth cap). A 5th finding wasn't a bug but a real coverage gap: every example only ever called a 6-parameter function as a top-level entry point, which exercises r8/r9 handling in the callee's *prologue* but never in a *caller's* argument-marshalling path — added a function that calls another with 6 computed arguments to close the gap. One limitation is disclosed rather than "fixed": Ember has no stack-depth guard, so extreme non-tail recursion overflows the real OS call stack with an uncatchable `SIGSEGV` — measured safe to ~100K recursion levels on this box's default 8 MiB stack, and verified that an equivalent `gcc -O0`-compiled C function segfaults at the identical depth for the identical reason, so this is native-code reality, not an Ember-specific defect. 123/123 tests green (lexer, parser precedence/associativity, interpreter wraparound/truncating-division semantics, the x86-64 encoder independently verified against real `objdump`, the full differential battery, all 5 review-finding regressions, the optimizer, and subprocess-driven CLI tests including clean-error-handling checks) plus `demo.sh` running the full suite, the CLI demo, and explicit CLI smoke checks end-to-end.

## 2026-08-11 — Bellman
- **What:** A from-scratch reinforcement-learning library and study — discrete gridworld MDPs (GridWorld, CliffWalking, WindyGridworld, FrozenLake) solved *exactly* by hand-written value iteration/policy iteration, tabular Q-learning/SARSA/every-visit Monte Carlo control checked for convergence against that exact ground truth, a from-scratch continuous-state cart-pole physics environment (the real nonlinear cart-pole ODE, semi-implicit Euler integration), and two structurally different function-approximation agents — a Deep Q-Network and REINFORCE — that learn to balance it from raw state. A genuinely new domain for this repo: every prior "learns" (nine from-scratch transformers, Cotangent's autodiff-driven NN, Loom/Sprout) learned by supervision against precomputed labels; nothing before this has learned purely from trial-and-error against its own consequences.
- **Stack:** Pure Python 3 stdlib only (no NumPy, no gym/stable-baselines, no PyTorch/TensorFlow/JAX) — the MDP solvers, physics, and a hand-derived 2-hidden-layer MLP (forward/backward/Adam, gradient-checked against finite differences to 1e-12) are all from scratch; self-contained HTML/Canvas/SVG for the interactive report (no CDN, no build step); Playwright/Chromium used only to verify the generated HTML.
- **Features shipped:** 4/4 required — discrete MDP envs with a full enumerable transition/reward model plus an exact DP oracle; tabular Q-learning/SARSA/Monte-Carlo (Q-learning matches the DP-optimal policy on 12/12 GridWorld states; the classic on-policy/off-policy CliffWalking divergence is reproduced and tested, not asserted — Q-learning's greedy policy walks the cliff edge, SARSA's stays away); from-scratch cart-pole physics; a Deep Q-Network (experience replay, target network, Double DQN target, state normalization, Huber loss) trained to a measured ~120-step average (vs. ~15-20 for random play). Both planned stretch features shipped: REINFORCE (Monte-Carlo policy gradient with a learned, normalized baseline) — which turned out to **measurably outperform DQN** on this task (~460/500 steps vs. ~120, both correctly implemented, gap explained honestly in REVIEW.md rather than tuned away) — and an interactive, theme-aware HTML report with real value-iteration heatmaps + policy-arrow overlays, replayable learned-agent-path animations, a Q-learning-vs-SARSA CliffWalking comparison, and side-by-side DQN/REINFORCE training curves with a replayable cart-pole swing animation.
- **Verdict:** Shipped. 4/4 required + 2/2 stretch. Adversarial review (Phase 3) found and fixed 3 real bugs: `CartPoleEnv.step()` crashed with a confusing raw `TypeError` if called before `reset()` (GridWorld already guarded this, CartPole didn't); the CLI accepted `--episodes 0`/negative and crashed deep inside `statistics.mean()` on an empty reward list instead of failing cleanly (fixed with a shared `positive_int` argparse type across every `--episodes` flag); and the HTML report's chart downsampler silently dropped the final (often most interesting) training-curve data point on episode counts that don't divide evenly into its bucket count, a float-rounding-at-the-boundary bug. Also documented two things that looked like bugs but weren't: REINFORCE's real, substantial performance edge over DQN on CartPole (a known-in-the-literature effect of full-episode Monte-Carlo returns vs. one-step TD bootstrapping through a small non-prioritized replay buffer, not an implementation defect — both agents are gradient-checked/oracle-checked correct), and REINFORCE deliberately not normalizing its input state the way DQN does. 70/70 tests green (DP-oracle hand-solved-MDP checks, transition-model probability-sums-to-one checks across every env, tabular exact-convergence-to-optimal checks, the CliffWalking divergence reproduction, NN gradients vs. finite differences, DQN/REINFORCE end-to-end training against hard numeric pass bars — DQN >70 mean eval reward, REINFORCE >250 — CLI subprocess smoke tests including clean-error-on-bad-input checks) plus `demo.sh` running the full suite, every CLI command, and a full-scale report build end to end.
