"""Folio: a browser rendering engine built from scratch.

Pipeline: HTML text -> DOM tree -> (CSS cascade) -> computed styles ->
layout tree (CSS2.1 box model) -> paint (SVG).
"""

__version__ = "0.1.0"
