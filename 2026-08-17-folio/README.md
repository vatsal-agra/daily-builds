# Folio

A browser rendering engine built from scratch in Python: HTML parser → DOM →
CSS cascade → computed styles → CSS2.1 box-model layout → paint, verified
against real headless Chromium.

**Status: Phase 1 (plan) complete.** See `PLAN.md` for the full architecture
and feature list. Implementation starts in Phase 2.
