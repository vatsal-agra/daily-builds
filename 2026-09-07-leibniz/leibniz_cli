#!/usr/bin/env python3
"""Entry point: run `./leibniz_cli <command> ...` from this directory (or
`python3 leibniz_cli <command> ...` from anywhere)."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from leibniz.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
